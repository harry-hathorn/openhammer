# OpenHammer — Implementation Plan

> A standalone MCP server with **no LLM** that mints a per-instance bearer token and exposes pi's 7 local shell/filesystem tools (`read`, `bash`, `edit`, `write`, `grep`, `find`, `ls`) to a remote agent over Fastify + stateless Streamable HTTP, rooted at `MCP_ROOT_DIR`, gated by the credential, with an optional cloudflared quick-tunnel.
>
> **Source of truth:** `specs/01`–`specs/21` (15 = testing strategy, 16 = containerized E2E, 17 = terminal app & TUI configuration, 18 = tool orientation `guide`, 19 = TUI control center, 20 = OAuth authorization server (client-credentials), 21 = UI consolidation (pi-tui only — drop clack/ora); sessions/memory deferred). Porting references: tool execute logic ← `/home/haz/source/pi/packages/coding-agent/src/core/tools/`; terminal/TUI/settings model ← `pi` (`packages/coding-agent/src/cli/args.ts`, `packages/coding-agent/src/core/settings-manager.ts`, `packages/tui`); session/execution harness ← `pi` (`packages/agent/src/harness/`, referenced by the deferred sessions work). **Spec `99` (agent harness) is explicitly OUT OF SCOPE for v1 — no tasks from it here.**

## How to read this plan

- Each `- [ ]` checkbox is **ONE iteration** for a fresh-context AI — one file (or one small cohesive change to a single file), tests folded in.
- Tasks are in **strict dependency order**; pick the first unchecked box. Each item lists: spec ref, target file, porting source, deps (item numbers), and a one-line scope.
- Validation trio must pass before checking off any task: `npm test` (vitest `--passWithNoTests`; hermetic Tiers 0–2 — units + in-process MCP E2E + boot E2E), `npm run typecheck` (`tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit` — covers `src` **and** `test`), `npm run lint` (`biome check src test`). Containerized tiers (`test:compose`, `test:in-container`, gated `test:tunnel`) are on-demand/non-blocking — see specs 15/16 and the **Testing & pipeline** section below.
- **Conventions (apply everywhere — see `docs/coding-standards.md`):** `.ts` relative import extensions; `import type` for type-only imports; `node:` on every built-in; tabs / indent 3 / lineWidth 120 / double quotes; **Result error model** (tool `execute` → `Promise<Result<ToolOk, Error>>`; expected failures return `err(new Error(msg))`, never throw; the MCP `CallTool` handler is the single narrowing point); plain `Error` (no custom error classes).

## Status snapshot

**Scaffold done (spec 01, `01-scaffold-a`–`d`):** `package.json`, `tsconfig.json`, `biome.json`, `.env.example`, `vitest.config.ts`, and `src/` are in place. **`01-config` done:** `src/config.ts` + `src/config.test.ts` shipped (first source file + first tests); the earlier `TS18003`/`biome "no files"` transient is cleared and the full trio is green. No `test/` dir yet — first E2E tests land in `T-harness`/`T-canary`.

### Project scaffold (spec 01)

- [x] **01-scaffold-a.** `package.json` (deps `fastify`/`@fastify/cors`/`@modelcontextprotocol/sdk`; devDeps `typescript`/`tsx`/`vitest`/`@types/node`/`@biomejs/biome`/`pino-pretty`; scripts build/start/dev/test/typecheck/lint/format; `engines.node >=20`) + `npm install` — spec 01
- [x] **01-scaffold-b.** `tsconfig.json` (NodeNext, `.ts` import extensions via `allowImportingTsExtensions`+`rewriteRelativeImportExtensions`, `verbatimModuleSyntax`, `erasableSyntaxOnly`, strict knobs) — spec 01
- [x] **01-scaffold-c.** `biome.json` (tabs, indentWidth 3, lineWidth 120, double quotes; `useConst`/`useNodejsImportProtocol`/`noExplicitAny`=error, `noNonNullAssertion`=off; `organizeImports:on`) — spec 01
- [x] **01-scaffold-d.** `.env.example` (PORT/HOST/MCP_ROOT_DIR/MCP_AUTH_TOKEN/MCP_MAX_RESPONSE_BYTES/LOG_LEVEL) + `vitest.config.ts` + empty `src/` skeleton (subdirs created by their first file) — spec 01

---

## Build tasks (in dependency order)

### Config (spec 01)

- [x] **01-config.** `src/config.ts` — `Config` interface + `loadConfig(env = process.env)` (coerce PORT/MCP_MAX_RESPONSE_BYTES with `Number()`; `rootDir = path.resolve(MCP_ROOT_DIR || process.cwd())`; defaults port 3000 / host `127.0.0.1` / maxResponseBytes 512000 / logLevel `info`; do **not** fail boot if `rootDir` missing). + unit tests (`loadConfig({})` → defaults; `loadConfig({PORT:"4242",MCP_ROOT_DIR:"/tmp/x"})` → coerced). *deps: 01-scaffold-a.*

### Shared utilities (spec 02) + tool types (spec 10-types)

- [x] **02a.** `src/tools/result.ts` (NEW) — `Result<T,E=Error>` + `ok`/`err`/`map`/`andThen`/`getOrElse`/`combine`; pure, no imports. + tests (success propagation, `combine` short-circuit on err). *deps: none. Most-imported module — do first.*
- [x] **02c.** `src/tools/truncate.ts` — verbatim port from `pi/.../tools/truncate.ts` (`DEFAULT_MAX_LINES=2000`, `DEFAULT_MAX_BYTES=50*1024`, `GREP_MAX_LINE_LENGTH=500`, `formatSize`, `truncateHead`, `truncateTail`, `truncateLine`, types). + tests (`truncateHead` 3000→2000 lines; 60KB single line → `firstLineExceedsLimit`; `truncateTail` keeps last 2000; `formatSize(512*1024)`→`"512.0KB"`). *deps: none.*
- [x] **02d.** `src/tools/path-utils.ts` — verbatim port from `pi/.../tools/path-utils.ts` (`expandPath`, `resolveToCwd`). **Do NOT port `resolveReadPath`'s macOS screenshot variants.** + tests (`resolveToCwd("~/x","/root")`→`/root/x`; relative→under cwd; absolute passes through). *deps: none.*
- [x] **02b.** `src/tools/io.ts` (NEW) — Result-wrappers over `node:fs`/`node:fs/promises` (**fs surface only — NOT spawn**): `readFile`/`access`/`stat`/`statSync`/`readdir`/`readdirSync`/`writeFile`/`mkdir`/`exists`. + tests (success + errno-failure → `err` with `Error`). *deps: 02a.*
- [x] **02e.** `src/tools/output-accumulator.ts` — verbatim port from `pi/.../tools/output-accumulator.ts`. **Change `tempFilePrefix` `"pi-output"` → `"openhammer"`.** + tests (feed >50KB then `snapshot({persistIfTruncated:true})` → truncated tail + non-empty `fullOutputPath`). *deps: 02c.*
- [x] **10-types.** `src/mcp/types.ts` (NEW) — `ToolContent` (text|image), `ToolOk`, `ToolModule` (name/description/inputSchema/`execute(args,rootDir)→Promise<Result<ToolOk>>`), `McpToolEntry` (`{tool, handler}`). Imports `Tool` from `@modelcontextprotocol/sdk/types.js`, `Result` from `../tools/result.ts`. + tests (type-level; can be a trivial smoke test). *deps: 02a. Precedes every tool module — they all export a `ToolModule`.* **Deviation recorded:** `ToolModule.inputSchema` is typed `Tool["inputSchema"]` (SDK's JSON-Schema-object type), **not** `Record<string, unknown>` — the SDK's `Tool.inputSchema` requires `type:"object"` as a literal, so the spec's literal `Record<string, unknown>` (which has `type: unknown`) made the spec's own `createAllTools` fail `tsc`. Reusing `Tool["inputSchema"]` is single-source-of-truth and lets `createAllTools` lift it onto `tool.inputSchema` with **no `as` cast** (10-index ships as-written). Its `[x:string]: unknown` index keeps it permissive for every tool's hand-written inline literal. Spec 10 updated to match.

### Tools (specs 03–09)

- [x] **03a.** `src/tools/read.ts` — port `execute` text path from `pi/.../tools/read.ts`: `resolveToCwd` → access → UTF-8 read → `split("\n")` → offset/limit slice → `truncateHead` + continue/limit notices. **Strip** pi's `resolveReadPath`/compact-read/`getReadmePath`/theme/resize/magic-byte detection. **No line numbers.** + tests (full read, 3000-line→offset notice, offset/limit window, offset-past-EOF→err, missing→err). *deps: 02c, 02d, 10-types. Port source: `pi/.../tools/read.ts`.* **Convention set:** each `ToolModule` is a named export named `<tool>Tool` (e.g. `readTool: ToolModule`) — `10-index` imports the 7 this way. Image path (03b) not included here.
- [x] **03b.** `src/tools/read.ts` (2nd pass) — add image content-block path: extension detection (`.png`/`.jpg`/`.jpeg`/`.gif`/`.webp`) → base64 image block + `Read image file [mime]` text note; no resize (oversized caught by `MAX_RESPONSE_BYTES`). + tests (`pic.png` → text+image blocks). *deps: 03a.*
- [x] **04.** `src/tools/bash.ts` — port `execute` + local exec body from `pi/.../tools/bash.ts`: shell `$SHELL||"bash"`, spawn `[shell,"-c",cmd]` (`cwd:rootDir`, `detached` off-win32, merged stdout+stderr into `OutputAccumulator({tempFilePrefix:"openhammer"})`, timeout/abort → `process.kill(-pid,"SIGKILL")`, exit≠0→`err`, tail-truncation footer with `Full output:` path, `(no output)` default). **Strip** `getShellConfig`/`trackDetachedChildPid`/`commandPrefix`/`spawnHook`/render. + tests (`echo hello && pwd`; `exit 3`→err; `yes|head -c 2000000`→tempfile footer; `sleep 30, timeout:1`→timeout err + dead child). *deps: 02c, 02e, 10-types. Port source: `pi/.../tools/bash.ts`.* **Deviation recorded:** `abort` is unreachable in v1 — `ToolModule.execute(args, rootDir)` carries no `AbortSignal`, so the abort-kill branch (spec step 4) is omitted; only the **timeout** path can kill the group. The kill mechanism itself (`process.kill(-pid, "SIGKILL")` on the detached group) is identical for both, so wiring abort later is just threading a signal through `execute`'s signature. Faithful to pi everywhere else: child-exit promise on the `exit` event (not `close`, avoiding pipe-FD hangs on detached grandchildren); throws → `err(...)` via a single `try/catch` narrowing at the end (spawn tools manage their own streaming, don't use `io.ts`). Orphan test uses `/proc/<pid>/stat` state (not `process.kill(pid,0)`) to distinguish a live orphan from an unreaped zombie.
- [x] **05.** `src/tools/write.ts` — port `execute` from `pi/.../tools/write.ts`: `resolveToCwd` → `mkdir(dir,{recursive:true})` → `writeFile(utf-8)` → `Successfully wrote ${len} bytes to ${path}`. **Strip** `withFileMutationQueue`/`WriteOperations`/render. + tests (parent-dir creation, overwrite, write→read round-trip, abs/rel resolution). *deps: 02d, 10-types. Port source: `pi/.../tools/write.ts`.*
- [x] **06a.** `src/tools/edit-diff.ts` — port the apply path only from `pi/.../tools/edit-diff.ts`: `stripBom`, `detectLineEnding`, `normalizeToLF`, `restoreLineEndings`, `normalizeForFuzzyMatch`, `fuzzyFindText`, `applyEditsToNormalizedContent`, `countOccurrences`, error builders. **`applyEditsToNormalizedContent` returns `Result<{baseContent,newContent},Error>` (never throws).** **Do NOT port** `generateDiffString`/`generateUnifiedPatch`/`computeEditsDiff` (no `diff` dep). + tests (match, not-found, duplicate, overlap, fuzzy-trailing-ws, no-change; BOM/CRLF preserved). *deps: 02a. Port source: `pi/.../tools/edit-diff.ts`.*
- [x] **06b.** `src/tools/edit.ts` — port `execute`+`validateEditInput`+`prepareEditArguments` from `pi/.../tools/edit.ts`: tolerate `edits` as JSON string / legacy `{oldText,newText}`; access R_OK|W_OK→`Could not edit file…`; read→stripBom→detectLineEnding→normalize→`applyEditsToNormalizedContent`→restoreLineEndings→write → `Successfully replaced ${edits.length} block(s) in ${path}.`. **Strip** render/`EditOperations`. + tests (single, multi-disjoint, non-unique→err, not-found→err, overlap→err, fuzzy, BOM/CRLF round-trip). *deps: 06a, 02d, 10-types. Port source: `pi/.../tools/edit.ts`.* **Deviation recorded:** pi's `validateEditInput` throws on empty/non-array `edits`; here it returns `Result<EditInput,Error>` (hand-narrows `edits[]` shapes + `path` string at the boundary, no zod) so `execute` composes it with zero try/catch — the Result-spine convention. `errnoCode(error)` reads `ErrnoException.code` via `"code" in error` + `typeof` narrowing (no `as`) to build the pi-faithful `Could not edit file: <path>. Error code: <code>.` message. Every validation failure path asserts the file is left unchanged.
- [x] **07a.** `src/tools/bin.ts` (NEW) — `isToolAvailable(name): boolean` via `spawnSync(name,["--version"],{stdio:"ignore"})`→exit 0. + tests (present + PATH-stripped→false). *deps: none. Shared by grep/find/tunnel.*
- [x] **07b.** `src/tools/grep.ts` — port `execute` from `pi/.../tools/grep.ts`: `isToolAvailable("rg")`→else `err` install hint; `resolveToCwd`; build rg args (`--json --line-number --color=never --hidden` + flags); spawn + parse NDJSON `match` events to cap `limit`; exit 1 on no-match is OK; format `path:line: text` (+ context block when `context>0`); `truncateHead` byte-cap + notices. **Replace `ensureTool` with `isToolAvailable`. Strip** render/`GrepOperations`. + tests (TODO matches; ignoreCase/literal; no-match; rg-missing→err; limit notice). *deps: 07a, 02c, 02d, 10-types. Port source: `pi/.../tools/grep.ts`.* **Deviation recorded:** the spawn lifecycle is split into a `runRipgrep()` helper that returns `Result<{matches,matchCount,matchLimitReached},Error>` — the `close` event resolves it directly, so `execute` `await`s + short-circuits with **zero try/catch** (the Result-spine convention; `bash` narrows at the end because its spawn rejects, grep's resolves). pi's `signal`/abort path is stripped like `bash` (unreachable — `ToolModule.execute` carries no `AbortSignal`); the limit-kill remains (`child.kill()` after the match cap). **The limit-kill arrives as `close` `code === null` (SIGTERM)**, so the non-(0|1) exit check is guarded by `!killedDueToLimit` (pi's `killedDueToLimit` flag) — without it the cap-kill is misread as an error. `isToolAvailable("rg")` precedes spawn; `spawn("rg", …)` resolves via PATH (no path resolution needed). Tests use `.ignore` (rg honors it universally, git-free) to exercise the ignore-filtering path the tool delegates to — rg only honors `.gitignore` inside a git repo, and `--hidden` would drag `.git/hooks/*` noise into a git-init'd temp dir.
- [x] **08.** `src/tools/find.ts` — port `execute` from `pi/.../tools/find.ts`: `isToolAvailable("fd")`→else `err`; `resolveToCwd`; build fd args (`--glob --color=never --hidden --no-require-git --max-results`); pattern-with-slash handling (`--full-path` + `**/` prefix rewrite); spawn→lines; relativize/POSIX/trailing-`/`; cap + notices. **Replace `ensureTool` with `isToolAvailable`. Strip** render/`FindOperations`. + tests (`**/*.ts`; basename `*.json`; no-match; fd-missing→err; limit notice). *deps: 07a, 02c, 02d, 10-types. Port source: `pi/.../tools/find.ts`.* **Deviation recorded:** like `grep`, the spawn lifecycle is split into a `runFd()` helper that returns `Result<{lines},Error>` and resolves on the `close` event, so `execute` `await`s + short-circuits with **zero try/catch** (the Result-spine convention). fd differs from rg in two ways that shape the close handler: (1) fd exits **0 even on no-match** (rg exits 1), so an empty result is signalled by empty output → `No files found`, not a non-zero exit; only non-zero **with no output** is an error (with output = partial success); (2) `--max-results` bounds the set at the fd side, so there is **no client-side kill** (grep's match cap kills mid-stream). `resultLimitReached` is therefore `relativized.length >= effectiveLimit`. pi's `signal`/abort path is stripped (unreachable — `execute` carries no `AbortSignal`). `isToolAvailable("fd")` precedes spawn. **Env note:** `fd` is a hard runtime + test dep (mirrors `rg` for `grep`) — installed on the dev box via `pacman -S fd` (10.4.2); the `T-dockerfile` image must `apt-get install fd-find` (Debian names the binary `fdfind`) and alias it to `fd`, or the Tier-0 find tests skip/fail in-container.
- [x] **09.** `src/tools/ls.ts` — port `execute` from `pi/.../tools/ls.ts`: `resolveToCwd`; `existsSync`→`Path not found`; `isDirectory`→else `Not a directory`; `readdirSync` + case-insensitive sort + `/` suffix on dirs + stat-skip; `(empty directory)`; `truncateHead` byte-cap + notices. **Strip** render/`LsOperations`. + tests (alpha sort + `/` + dotfiles; empty; file→err; missing→err; >500→notice). *deps: 02c, 02d, 10-types. Port source: `pi/.../tools/ls.ts`.* **Deviation recorded:** like `read`/`write`/`edit`, the fs calls go through `io.ts` Result-wrappers (`exists`/`statSync`/`readdirSync`) rather than raw `node:fs` — the AGENTS.md convention that keeps the body try/catch-free — with pi-faithful error messages (`Path not found`/`Not a directory`/`Cannot read directory`); per-entry `statSync` err → skip the entry (pi's dangling-symlink behavior). The `AbortSignal` plumbing and UI-only `LsToolDetails` are stripped (unreachable/irrelevant in v1, mirroring `bash`/`grep`/`find`). The entry-limit test uses an explicit small `limit` (not 501 minted files) to exercise the `entryLimitReached` branch deterministically — the `find.test.ts` precedent; a separate test isolates the byte-cap (`50.0KB limit reached`) branch with long-named entries.

### Registry (spec 10-index)

- [x] **10-index.** `src/tools/index.ts` — `createAllTools(rootDir): McpToolEntry[]`: import the 7 `ToolModule`s, map each to `{tool:{name,description,inputSchema}, handler:(args)=>m.execute(args??{},rootDir)}`. + test (`createAllTools("/srv")` → exactly 7 entries with names `read,bash,edit,write,grep,find,ls`; handler returns a `Result<ToolOk>`). *deps: 10-types + 03–09.* **Note:** the `tool` descriptor is built by explicit `{ name, description, inputSchema }` listing (not spread) so it is excess-property-clean against the SDK `Tool` type — `ToolModule.inputSchema` is already `Tool["inputSchema"]`, so it lifts with **no `as` cast** (the single-source-of-truth payoff from `10-types`). `MODULES` is a module-level `ToolModule[]` constant giving a stable `tools/list` order (`read,bash,edit,write,grep,find,ls`). Handler `(args?) => module.execute(args ?? {}, rootDir)` closes over `rootDir`; the test proves args-forwarding (bash echo) and the `args ?? {}` default (read with no path → `err`, never throws).

### Auth (spec 11)

- [x] **11a.** `src/auth/token.ts` — `ensureToken(config)`: if `config.authToken` set → return it (no file touch); else read `~/.openhammer/credential.json`; if valid reuse, else mint `crypto.randomBytes(32).toString("base64url")` + `createdAt` + `mkdir` + `writeFileSync({mode:0o600})`. Throw on unwritable dir. + tests (mint→~43-char + 0600; reuse; override; no-file-write when override set). *deps: 01-config.* **Deviation recorded:** signature is `ensureToken(config, credPath = credentialPath())` — an optional 2nd arg so tests inject a temp `credPath` instead of mutating `process.env.HOME`; the public `ensureToken(config)` form `main.ts` (14b) calls still uses `os.homedir()`. Also exports `credentialPath(homeDir = os.homedir())` for `printStartup` (14a) to surface the cred path without re-deriving it. This is a **boot-boundary** that THROWS on failure (unwritable cred dir → `main.ts` exits non-zero) — it uses raw `node:fs` + a wrapping `try/catch`, **not** `io.ts`'s `Result`-wrappers (those exist to keep *tool* bodies try/catch-free; `ensureToken` is the one throwing credential boundary). Dir created `0o700`, file `0o600` (both owner-only; the file mode is umask-safe — owner bits survive any umask). `isCredential` narrows `unknown` via `"token" in v` + `typeof` (no `as`); corrupt/empty-token files mint fresh. The unwritable-dir test blocks via ENOTDIR (a file on the path) rather than perms so it's root-safe (root bypasses perms, not ENOTDIR).
- [x] **11b.** `src/auth/middleware.ts` — `createAuthMiddleware(token, config): FastifyPreHandler`: parse `Authorization: Bearer <v>`; equal-length decode + `crypto.timingSafeEqual` (length-mismatch short-circuits); on miss/mismatch → `reply.code(401)` + `WWW-Authenticate: Bearer realm="openhammer", resource_metadata="<baseUrl>/.well-known/oauth-protected-resource"` + JSON-RPC error body. `baseUrl` from `${req.protocol}://${req.host}` with fallback `http://${config.host}:${config.port}`. + tests (no-auth→401; wrong→401; correct→proceeds; timing-safe length handling). *deps: 01-config.* **Deviations recorded:** (1) the return type is `preHandlerHookHandler` — Fastify **v4 has no `FastifyPreHandler` export** (only the lowercase `preHandlerHookHandler` interface, re-exported from the root); it's the single-source-of-truth hook type with all-default generics. (2) the hook is **`async`** — load-bearing: a synchronous arity-2 hook (no `done` callback) that returns `void` makes Fastify wait for a never-called `done()` and the request **hangs** (confirmed: success-path `inject` timed out at 5s until the hook returned a Promise). Returning a Promise is how Fastify advances the lifecycle. (3) `baseUrl` reads `request.headers.host` directly rather than `request.host` — Fastify **v4 exposes no `request.host`** (only `hostname`, which for HTTP/1.1 returns the raw `Host` header value incl. port anyway); reading the header narrows the `string | string[] | undefined` union honestly and keeps the "no-Host → config fallback" branch real. Expected token is encoded to a `Buffer` once at build time; `parseBearer` is case-insensitive on the scheme + whitespace-trims via `trim().split(/\s+/)` (exactly-2-parts or it's absent).
- [x] **11c.** `src/mcp/well-known.ts` — `registerWellKnown(fastify, baseUrl)`: `GET /.well-known/oauth-protected-resource` (no auth) → `{resource:"<baseUrl>/mcp", bearer_methods:["header"]}`. + test (200 + body, no auth). *deps: none.* **Deviation recorded:** the field is `bearer_methods`, **not** RFC 9728's `bearer_methods_supported`. This is the documented v1 "minimal subset" decision — spec 11 + this task agree on `bearer_methods` verbatim, so it ships as-specified; note the interop consideration: a strict RFC-9728 client keying on `bearer_methods_supported` would not recognize the method. Revisit when a real MCP client's discovery is verified end-to-end (Tier-1 `T-mcp-e2e` / manual smoke). The route is inherently unauthenticated — it's registered standalone, and `src/server.ts` (12c) attaches the auth `preHandler` to `POST /mcp` only, so discovery precedes auth.

### MCP server, transport, Fastify (spec 12)

- [x] **12a.** `src/mcp/server.ts` — `createMcpServer(rootDir, maxResponseBytes): Server`: `new Server({name:"openhammer",version:<pkg>},{capabilities:{tools:{}}})`; `ListTools` → `entries.map(e=>e.tool)`; `CallTool` → find by name (unknown→`isError` text), narrow `Result` with **fallback try/catch** (genuine bugs → `err`), apply universal size backstop on success (`sum Buffer.byteLength` text + image `data.length`; over cap → replace whole content with one `response_too_large` JSON text block). + tests (ListTools=7; unknown tool→isError; err→isError no-throw; backstop fires on >cap). *deps: 10-index, 02a, 10-types.* **Deviation recorded:** `version` is the single-source `pkg.version` from `import pkg from "../../package.json" with { type: "json" }` — NOT `process.env.npm_package_version ?? "1.0.0"` (which is unset under a bare `node dist/main.js` in the container); the JSON-import-attribute is the true source and resolves identically from `src/mcp/` (test) and `dist/mcp/` (prod). The CallTool backstop emits the spec's exact short message and returns **no `isError`** (a structured success-payload, per spec line 34). Tests drive the real `Server` via an SDK `Client` over `InMemoryTransport` (hermetic Tier-0 — no Fastify/port); the `callTool` return is an SDK `content | toolResult` union, narrowed with `"content" in result` + the `text` discriminant — **zero `as` casts** (guards carry the narrowing; `request.params.arguments` is already `Record<string,unknown>|undefined` so `entry.handler(args)` casts nothing). The `Result<ToolOk>` narrowing + fallback catch + backstop compose with one `let r` reassignment (the only mutable local) — the `try/catch` is a bug safety-net only; tools never throw for expected failures.
- [x] **12b.** `src/mcp/http-transport.ts` — `mcpHttpRoutes(fastify,{token,config})` plugin: `POST /mcp` with auth `preHandler` (only on POST) → per-request `createMcpServer` + `new StreamableHTTPServerTransport({enableJsonResponse:true})` (**no `sessionIdGenerator`**), `reply.raw.once("close")` cleanup, `server.connect(transport as unknown as Transport)`, flush Fastify headers onto `reply.raw`, `transport.handleRequest(req.raw, reply.raw, req.body)`, `return reply.hijack()`. `GET /mcp`→405, `DELETE /mcp`→405. + integration test against a real Fastify (tools/list via transport; auth gating; 405s). *deps: 12a, 11b.* **→ activates Tier-1 in-process E2E** (`T-mcp-e2e`): a real SDK `Client` drives all 7 tools over `POST /mcp` on port 0, asserts auth 401 + the `MAX_RESPONSE_BYTES` backstop. **Deviation recorded:** (1) the `reply.raw.once("close", …)` teardown is `void transport.close().catch(()=>{}); void server.close().catch(()=>{})` — **not** an empty `try { … } catch {}` — because AGENTS.md forbids empty catch blocks; per-promise `.catch(()=>{})` is the repo's established idiom (`server.test.ts`) and is semantically identical best-effort teardown. (2) `mcpHttpRoutes(fastify, opts)` is `async` arity-2, so 12c can either call it directly (`await mcpHttpRoutes(fastify, {token, config})`) or register it as a plugin (`fastify.register(mcpHttpRoutes, {token, config})`) — both pass `opts` as the 2nd arg and keep the routes on the parent scope (no `fastify-plugin` dep needed). (3) the header-flush loop needs **no cast** — Fastify v4's `reply.getHeaders()` value union (`number|string|string[]|undefined`) is assignable to Node's `ServerResponse.setHeader` param (`number|string|readonly string[]`) once the `!== undefined` guard narrows it (no `as` cast needed). (4) the integration test (`src/mcp/http-transport.test.ts`) drives a real SDK `Client` over `StreamableHTTPClientTransport` against a **listening port-0 Fastify** (the client uses its own `fetch`, so `inject` can't satisfy it) for `tools/list`→7 tools, with `inject` for the 401 (POST, no bearer) + GET/DELETE 405s; the bearer rides on every client request via `requestInit.headers` (no `authProvider`, so the SDK never starts the OAuth discovery flow).
- [x] **12c.** `src/server.ts` — `buildFastify(config, token): Promise<FastifyInstance>`: `Fastify({logger:{level, transport:pino-pretty in dev}})`; `@fastify/cors` (`origin:true,credentials:true,methods:[...],exposedHeaders:["Mcp-Session-Id","Mcp-Protocol-Version","WWW-Authenticate",...]`); `GET /health`→`{status:"ok"}`; `registerWellKnown(fastify, baseUrl)`; register mcp routes plugin; global error + 404 handlers; **return `fastify` WITHOUT calling `listen`** (spec 12 edit — `main.ts` owns binding/lifecycle; this is what lets Tier-1 tests bind port 0 and Tier-2 tests control shutdown). + test (`buildFastify(...)` then `listen({port:0})` → `GET /health` 200 no-auth; close cleanly). *deps: 01-config, 11c, 12b.* **Deviation recorded:** (1) `mcpHttpRoutes` is **called directly** (`await mcpHttpRoutes(fastify, { token, config })`), not `fastify.register(mcpHttpRoutes, …)` — it is already an async arity-2 function (the 12b note's option 1), so the `await` is meaningful (awaits the real async body) and the `/mcp` routes land on the parent scope alongside `/health` + well-known, with no plugin wrapper or `fastify-plugin` dep. (2) the pino-pretty transport is gated on `process.env.NODE_ENV === "development"` — production gets raw JSON; tests run with `NODE_ENV=test` (vitest default) so they take the plain branch and never spawn a pretty-print worker thread. Note: `npm run dev` (`tsx watch`) does not currently set `NODE_ENV=development`, so pretty logs won't activate via it until the dev script/main.ts sets it — a 14b polish, out of scope here. (3) the error handler's `statusCode` is narrowed honestly via `"statusCode" in errorInput && typeof … === "number"` — **no `as`** (AGENTS.md forbids `as` outside the documented SDK/pi exceptions); the `/mcp` POST's `reply.hijack()` means its errors bypass this handler, so it only catches pre-hijack bad-request/throw paths. (4) the well-known `baseUrl` is the configured `http://${config.host}:${config.port}` (build-time) — distinct from the per-request `Host` the auth `WWW-Authenticate` challenge uses (spec 11), so under an ephemeral listen port the advertised `resource` carries `config.port` (a known, accepted v1 limitation per 11c).

### Tunnel (spec 13)

- [x] **13.** `src/tunnel/cloudflare.ts` — `startTunnel(port): Promise<{url,child}|null>`: `isToolAvailable("cloudflared")`→else `null` (no throw); `spawn("cloudflared",["tunnel","--url",`http://localhost:${port}`,"--no-autoupdate"])`; regex-scan **stderr** for `https://[a-z0-9-]+\.trycloudflare\.com`; ~15s timeout→kill+`null`; early child death→`null`; return `{url,child}`. + tests (presence-check branches; URL regex parsing; mock spawn). *deps: 07a.* **→ activates Tier-4 tunnel E2E** (`T-tunnel-e2e`): gated `npm run test:tunnel` spins up the compose `tunnel` profile and drives a `tools/call` round-trip through the public `trycloudflare.com` URL. **Deviation recorded:** `startTunnel(port, opts?)` takes an optional 2nd arg — mirrors the `11a` injection-arg precedent — so the public `startTunnel(config.port)` call `main.ts` (14b) will make stays clean while tests inject `spawn`/`isAvailable`/`timeoutMs`/`onLog`. The URL regex is lifted into a pure exported `extractTunnelUrl(stderr): string | null` (unit-tested directly; re-scans the accumulated stderr buffer each chunk so a URL split across chunk boundaries is still caught). Tests inject a `spawn` that runs **real `node -e` subprocesses** writing deterministic fake URLs to stderr — exercising the actual `node:child_process` spawn/stderr-streaming/`close` plumbing hermetically (no `vi.mock`, **no `as` casts**: the injected `spawn` is typed `(args) => ChildProcess`, so the default and the test fake share one concrete type and `startTunnel` touches real `child.stderr`/`on`/`kill`/`killed`). Like `grep`/`find`, the lifecycle is a single `new Promise` with a `settled` flag (settle-once: a `close` after the URL is found is a no-op, can't override success with `null`). `null` is returned (not `err`) for every failure mode — this is a graceful optional feature, not a tool execution, so it deliberately stays **off** the `Result` spine. The ~15s timeout is the only path that kills the child: the absent-binary branch short-circuits *before* spawn, and the early-death `close`/`error` handlers find the child already gone (no kill needed). `opts.onLog` forwards pre-URL stderr chunks for diagnosability (`main.ts` wires it to the logger in 14b); `cloudflared` is absent on the dev box, so the default presence-check path returns `null` naturally (also covered by a PATH-stripped test mirroring `bin.test.ts`).

### Boot (spec 14)

- [x] **14a.** `src/startup-print.ts` — `printStartup({localUrl, tunnelUrl, token})`: print local `/mcp` URL, tunnel URL (if any), token (once), ready-to-paste `mcpServers` JSON (`{"type":"http","url":"<localUrl>/mcp","headers":{"Authorization":"Bearer <token>"}}`), credential-file path + reuse note. + tests (emits URL + token + parseable `mcpServers` JSON; tunnel URL omitted when absent). *deps: none.* **Deviation recorded:** the spec's `printStartup(...): void` is kept as the public `main.ts` (14b) call, but the pure formatting is split into two exported seams so it is unit-testable **without spying on the console**: `mcpServersConfig(endpointUrl, token)` (the exact `mcpServers` object the spec block shows — single source for the block **and** its test) and `formatStartup(info): string` (the full banner). `printStartup` is the trivial `console.log(formatStartup(info))` wrapper. The `mcpServers` block points at **`localUrl`** (the spec's verbatim `<localUrl>/mcp`), even when a `tunnelUrl` is present — the local block is the spec's pasteable artifact; the tunnel endpoint is printed on its own line. The credential path is surfaced via `credentialPath()` from `11a` (the function that note said it was exposed for) — printed as the real absolute path (e.g. `/home/u/.openhammer/credential.json`), the single-source value, rather than the spec's illustrative `~/.openhammer/credential.json`. Tests assert the banner `.toContain`s the exact `JSON.stringify(mcpServersConfig(...), null, 2)` substring (so "parseable mcpServers JSON" is proven by identity, not fragile line-slicing), and a `console.log` spy proves `printStartup` writes it once.
- [x] **14b.** `src/main.ts` — `loadConfig()` → `ensureToken(config)` → parse argv `--tunnel` → `buildFastify(config,token)` → **`await fastify.listen({port:config.port,host:config.host})`** → if tunnel `startTunnel(config.port)` (null→log localhost-only) → `printStartup(...)` → `SIGINT`/`SIGTERM` handlers (`fastify.close()`, kill `t?.child`, `process.exit(0)`). *deps: 01-config, 11a, 12c, 13, 14a.* **→ activates Tier-2 boot E2E** (`T-boot-e2e`: spawn the entrypoint, assert `/health`, token reuse across two boots, clean `SIGINT`/`SIGTERM`) **and Tier-3 real compose** (`T-real-compose`: `test:compose:real` retargets the runner at the real `server` service for all 7 tools). The manual `npm start` smoke remains as a one-time human check. **Deviation recorded:** structure is an `async function main()` driven by `main().catch(...)` (no top-level await) — the single boot boundary, matching AGENTS.md ("exceptions stay at boot boundaries"). Boot failures (`ensureToken`'s unwritable cred dir, `EADDRINUSE` on listen) reject `main` → the `.catch` writes a one-line actionable `console.error` + `process.exit(1)`; a clean `SIGINT`/`SIGTERM` exits 0. The tunnel result is `wantTunnel ? await startTunnel(config.port) : null` so the `TunnelResult | null` type infers without importing the type; the absent-binary warn guards on `wantTunnel && tunnel === null` and goes through `fastify.log.warn` (pino structured log, not the banner). Shutdown is one-shot idempotent — a `shuttingDown` flag absorbs a second signal mid-shutdown (Ctrl+C twice), the async handler rides behind `void shutdown(...)` (no floating promise), and follows the spec order `await fastify.close()` → `tunnel?.child.kill()` → `process.exit(0)` (no orphan tunnel child). No co-located unit test: per spec 14's acceptance criteria, `main.ts` wiring is verified by the separate `T-boot-e2e` boot-E2E checkbox (its own iteration); this iteration ships it verified by the hermetic trio (compiles/lints) plus a hermetic boot smoke that confirmed `/health` 200, the well-known body, 401-without-bearer / 200-with-bearer on `/mcp`, **token reuse identical across two boots**, clean `SIGTERM` exit 0 on both, and the tunnel-absent warn line.

### Terminal app & TUI configuration (spec 17)

Grow OpenHammer from `npm start` into a **`openhammer` CLI** that mirrors pi's terminal-driven model: an interactive **TUI** (on `@clack/prompts`) that prints the README banner and drives configuration through a **schema-driven wizard**, backed by a persisted **settings doc** + separate **credentials store**, a swappable **provider registry** (factory), and a **doctor** diagnostics registry. **Channels are the first domain (live: ngrok/cloudflare; static/deployed: nginx/fixed-URL) and an MCP allowed-clients section is the second; the wizard/registry/schema are domain-agnostic so further domains reuse them** — the scalability seam. **Start with the TUI** (17a–17c). Spec: `specs/17`; deps confirmed on public npm (`@clack/prompts`, `@ngrok/ngrok`, `ora`). Every checkbox ships green on the hermetic trio (`npm test` covers the new Tier-0 units + wizard logic + registries + settings round-trips + doctor + telemetry); the ngrok public-URL sweep is gated (T-ngrok-channel-e2e). Convention reminders: Result model for domain ops; `null` (not `err`) for graceful-absent; the CLI is the boot boundary (throws → actionable stderr + non-zero exit); atomic `0700`/`0600` writes (spec-11 precedent); `ConfigField`/`ChannelKind`/`ChannelMode` are `const`-object + derived unions (erasableSyntaxOnly).

- [x] **17a.** `package.json` (add `@ngrok/ngrok` to **dependencies**, `@clack/prompts` + `ora` to **devDependencies**; add `"bin": { "openhammer": "dist/cli.js" }` + chmod `dist/cli.js` on build) + **`src/tui/banner.ts`** — `BANNER` = the README lines 1–22 ASCII block (single source) + `printBanner(stream)`. + test that parses the first fenced `text` block out of `README.md` and asserts `BANNER` equals it byte-for-byte (refuses drift between README and TUI). *deps: 01-scaffold-a.* **Deviation recorded:** the TUI substrate is **`@clack/prompts`**, not pi's `packages/tui` — openhammer is a no-LLM config tool, so ~half of pi-tui (`Markdown`/`marked`, the prompt `Editor`, slash-`autocomplete`, Kitty/iTerm2 `Image`) is permanent dead weight. The Node.js CLI best-practices §2.1 (*Prefer a small dependency footprint*) makes this a mandate, not an exception: `@clack/prompts` (MIT, 6 runtime deps) covers select/text/password/confirm; `ora` covers the async probe spinner (§1.5). pi stays the behavioral reference, clack the substrate. All three deps verified on public npm before this task was written. **Shipped:** `@ngrok/ngrok@^1.7.0`→dependencies (lazy-`import()`ed only in the 17i ngrok provider, so the native binding never loads at boot), `@clack/prompts@^1.6.0` + `ora@^9.4.1` (ora 9 is ESM-only — fine, the project is `"type":"module"`)→devDependencies; `npm install` added 25 pkgs + synced `package-lock.json` (npm **preserved** `package.json`'s tab indent); `"bin": { "openhammer": "dist/cli.js" }`. **Build script** is now `tsc && { chmod +x dist/cli.js 2>/dev/null || true; }` — the `{ … || true; }` group runs only after `tsc` succeeds (so a `tsc` failure still propagates non-zero) and tolerates `chmod` failing while `dist/cli.js` is absent (no `src/cli.ts` until 17n); once 17n ships `src/cli.ts`, `tsc` emits `dist/cli.js` (preserving its `#!/usr/bin/env node` shebang) and the chmod makes the `bin` executable. **`BANNER`** is a **JSON-escaped** `string` literal (not a hand-typed template literal) so every byte survives verbatim — runic `ᚦᛟᚱᛞ`, box-drawing, and the **truly-empty** separator line (README line 15, confirmed LF + zero trailing whitespace via `cat -A` before mirroring). Biome wrapped the assignment across two lines (the literal line still exceeds `lineWidth:120` but is an unbreakable token it leaves alone). **`printBanner(stream: BannerStream = process.stdout)`** takes a minimal structural `BannerStream { write(chunk: string | Uint8Array): boolean }` (`process.stdout` satisfies it) so the test injects a plain fake object — no `node:stream` dep in the unit test; it writes `` `${BANNER}\n` ``. The drift test compares `Buffer.from(BANNER,"utf8")` vs `Buffer.from(<README block>,"utf8")` — the literal byte-for-byte interpretation — plus a `printBanner` stream assertion; 3 tests, the trio green (264 passed / 1 skipped).
- [x] **17b.** `src/tui/prompts.ts` — thin adapters over `@clack/prompts`: `askSelect(options)`, `askText(label)`, `askSecret(label)` (masked), `askConfirm(label)` (each → `Promise<string|boolean|null>`, `null` = cancel) + `withSession(title, fn)` (prints the banner via `printBanner` + clack `intro`/`outro`). **No custom render loop** — clack owns TTY/raw-mode rendering (no `app.ts` to build). + tests with an injected fake `io` (logic split from rendering; clack's TTY requirement never touches the unit tests). *deps: 17a.* **Deviation recorded:** the injectable seam is a `PromptIo` interface whose methods resolve to `T | null` (`null` = cancel) — **not** clack's `T | symbol`. clack's `CANCEL_SYMBOL` is a private module-scoped symbol (`@clack/core`, checked by identity in `isCancel`) that is **unreachable in a unit test**, so a fake `io` could never produce a value `isCancel` recognizes. Resolving the `isCancel → null` translation inside the production `defaultIo` (the single clack boundary) fixes this: everything above `defaultIo` — the 4 adapters + `withSession` — is pure orchestration over a clean `T | null` contract and is fully unit-tested with a recording fake `io` that just returns `null` to simulate cancel; clack's TTY (and its private cancel symbol) never enter the hermetic trio. The `isCancel` 1-liner itself is exercised by the TUI E2E tiers (T-cli-e2e), consistent with "exceptions/rendering stay at boundaries." Each adapter takes an optional `io: PromptIo = defaultIo` last arg (production call sites omit it → spec-faithful `askSelect(options)`/`askText(label)`; tests pass the fake) — mirrors the `11a`/`13` injection-arg precedent. `withSession<T>(title, fn, deps: SessionDeps = {})` adds the optional `deps = { io?, stream? }` (the banner destination defaults to `process.stdout`, structurally satisfying `BannerStream` from `17a`); it does `printBanner(stream) → io.intro(title) → await fn() → io.outro()` in a `try/finally` so `outro` (clack UI cleanup) **always** runs — even when `fn` throws (it rethrows after `outro`) or returns `null` (a cancelled inner prompt) — the cleanup logic that's the meaningful, tested behavior here. The fake `io` records every call (`calls.select/text/password/confirm/intro/outro`) so the tests assert both forwarding (the right primitive + the label→`{message}` mapping) and orchestration (banner printed byte-for-byte via a fake `stream` mirroring `banner.test.ts`, `intro` called with the title, `outro` called once) — 11 tests, trio green (275 passed / 1 skipped). The io's `text`/`select`/`confirm` take local option-object types (`AskTextOptions`/`AskSelectOptions`/`AskConfirmOptions`) rather than clack's generics, so fakes are trivial to write **and** 17c's wizard can reuse `defaultIo` directly to pass field defaults without a parallel `WizardIo` type.
- [x] **17c.** `src/tui/schema.ts` (`ConfigField` discriminated union: `text` | `secret` | `select` | `confirm`, with `options`/`default`/`required`/`help`) + `src/tui/wizard.ts` (`runWizard(title, fields, io): Promise<Record<string,string> | null>` — renders the banner then each field via its primitive; `io` injectable). A pure `reduceFields(fields, rawAnswers)` does the field→value reduction so the state machine is unit-tested without a terminal. + tests (field-kind dispatch, required-empty → `null`, cancel propagation). *deps: 17b.* **This is the "configure other things later" engine — generic over `ConfigField[]`; a future domain declares fields and reuses `runWizard` unchanged.** **Deviation recorded:** `WizardIo` is `export type WizardIo = PromptIo` — a zero-cost alias, not a parallel interface — so the spec's `io: WizardIo` name is honored while `defaultIo` passes straight through (the 17b note's "reuse `defaultIo` directly … without a parallel `WizardIo` type"). `reduceFields` is the pure reducer (default application; confirm → `"true"`/`"false"`; required-empty → `null`); it folds the `kind === "confirm"` branch first then a single text/secret/select path, so **no switch / exhaustiveness machinery** is needed in the reducer. The `kind`→primitive dispatch lives in a private `promptField` switch with a `default: { const _exhaustive: never = field; return _exhaustive; }` guard (required by `noImplicitReturns` + `noFallthroughCasesInSwitch`). `runWizard` **reuses 17b's `withSession`** for the banner + `intro`/`outro` framing (single source) and threads each field's `default` through the io — verified against `@clack/prompts`'s `TextOptions.defaultValue` ("a fallback value returned when the user provides nothing"), so 17b's "reuse `defaultIo` to pass field defaults" intent lands exactly; `reduceFields` remains the authoritative backstop. Signature is `runWizard(title, fields, io = defaultIo, deps: WizardDeps = {})` — a 4th `deps.stream` injection seam (mirrors 17b's `SessionDeps`) keeps the 4 `runWizard` tests hermetic via a fake `BannerStream` (no stdout pollution), the `11a`/`13`/`17b` injection-arg precedent. **Cancel vs required-empty are distinct concerns:** cancel (a field's io call resolves `null`) propagates in `runWizard` — it stops walking on the first cancel and never calls `reduceFields`; required-empty is `reduceFields`'s pure job. Tests use a recording fake io with **per-kind answer arrays** (mirrors `prompts.test.ts`'s per-method shape, so each primitive returns only its own type — **zero `as` casts**; the `take` helper narrows `arr.shift()` via `=== undefined`, no cast). 11 tests (7 `reduceFields` pure + 4 `runWizard`: dispatch+default-threading, cancel-stop, required-empty, banner/intro/outro framing); trio green (286 passed / 1 skipped).
- [x] **17d.** `src/config/settings.ts` — `Settings` / `ChannelEntry` / `ChannelKind` / `ChannelMode` / `McpSettings` types; `settingsPath(homeDir = homedir())` → `~/.openhammer/config.json`; `loadSettings(path?)` (absent or corrupt → defaults `{version:1, channels:[], defaultChannel:null, mcp:{allowedClients:[]}}`); `saveSettings(path, s)` (**atomic** temp + `rename`, dir `0700`, file `0600` — the spec-11 `writeCredential` hygiene). + tests (round-trip, defaults, corrupt → defaults, atomic write). *deps: 11 (reuses the `~/.openhammer` dir).* **XDG deviation (§1.3):** keep everything unified under `~/.openhammer` (consistent with the shipped `credential.json`) rather than `~/.config/openhammer` — documented; `OPENHAMMER_CONFIG_DIR` override is a later option. **Deviation recorded:** `ChannelMode`/`ChannelKind` are exported as `const` objects (`CHANNEL_MODES`/`CHANNEL_KINDS`) + derived unions — no `enum` (`erasableSyntaxOnly`) — and the loader's validation sets are `new Set(Object.values(...))` from the **same** objects, so adding a kind is one edit and the type, the validator, and (later) the registry all agree (single source of truth). Validation is **whole-doc and strict**: a `normalizeSettings(parsed)` guard returns `Settings | null` by hand-narrowing (`"version" in o`/`typeof`/`Array.every(isChannelEntry)` — **zero `as` casts**); any structural miss — corrupt JSON, wrong top-level shape, a channel missing `mode`/`options`, or an **unknown kind** — falls back to `defaultSettings()` (the spec's "absent or corrupt → defaults", taken at whole-doc granularity so a dangling `defaultChannel` can't survive a partially-malformed doc; `doctor` (17p) flags corrupt). `loadSettings` **never throws**; `saveSettings` **throws** with a dir/file-scoped message on `mkdir`/write/`rename` failure — the spec-11 `writeCredential` posture, and like `ensureToken` it uses **raw `node:fs` + a wrapping `try/catch`** rather than `io.ts`'s `Result`-wrappers (those exist to keep *tool* bodies try/catch-free; this is the throwing persistence boundary, not a tool). Atomicity = write a same-dir temp (`${basename}.${process.pid}.tmp`, mode `0600`), then `renameSync` over the target (atomic on one filesystem — a crash can't leave a truncated `config.json`); on a `rename` failure the temp is best-effort `rmSync({force:true})`-cleaned so no `.tmp` lingers, then the rename error is thrown. No `fsync` — faithful to the spec's "temp + rename" wording (parity with `writeCredential`, which also doesn't fsync); add it in a durability pass if crash-safety beyond torn writes ever matters. Pretty-printed JSON (`JSON.stringify(s, null, 2)`) for operator readability; `version` is preserved verbatim on load (a structurally-valid v2 doc loads; doctor can flag a version mismatch later). Secrets never live inline — a channel's `id` doubles as the `credentials.json` key (17e). Coexists with `src/config.ts` (file + `src/config/` subdir resolve independently under NodeNext). Tests: 17 covering `settingsPath`, the const objects, defaults, `loadSettings` (absent/corrupt/wrong-shape/malformed-entry/unknown-kind/valid-doc), and `saveSettings` (round-trip, `0600`/`0700` perms, no-temp-linger, atomic overwrite + no temps, ENOTDIR mkdir-failure throws without creating the target — the `11a` root-safe `blocker` file trick).
- [x] **17e.** `src/config/credentials.ts` — a separate `~/.openhammer/credentials.json` (`0700` dir / `0600` file) keyed `credId → Record<key,string>`; `getCredentials(id)` / `setCredentials(id, values)` / `deleteCredentials(id)`. + tests (isolated from `config.json`, owner-only perms). *deps: 17d.* **Mirrors pi's OAuth-credential split: secrets never live inline in `settings` — `config.json` holds only a `credId` reference.** **Deviation recorded:** strict sibling of `settings.ts` (17d) — the throwing persistence boundary. Atomic write (temp `${basename}.${pid}.tmp` `0600` + `renameSync`, dir `0700`, no-temp-linger on rename failure), raw `node:fs` + wrapping `try/catch` (**not** `io.ts` — those keep *tool* bodies try/catch-free; this is the throwing persistence boundary, not a tool). `getCredentials` **never throws** (absent/unreadable/structurally-corrupt file → `{}` for the id — a `doctor`-flaggable soft failure, never a boot error); `setCredentials`/`deleteCredentials` **throw** with a dir/file-scoped message on write failure (the spec-11 posture, surfaced as actionable stderr by the CLI). API: `credentialsPath(homeDir = homedir())` → `~/.openhammer/credentials.json`; `getCredentials(id, path = credentialsPath()): CredentialValues`; `setCredentials(id, values, path): void`; `deleteCredentials(id, path): void` — all **sync** (parity with `saveSettings`/`loadSettings`; they only do fs). The optional `path = credentialsPath()` injection-arg is the `11a`/`13`/`17d` precedent so tests inject a temp path without mutating `process.env.HOME`. **`getCredentials` returns `Record<string,string>` (`{}` for absent)**, not `undefined` — the sibling-consistent "absent → safe empty default" (`loadSettings` returns `defaultSettings()`), so a provider reading `getCredentials(id).authtoken` gets `undefined` until set with no `?? {}` at the call site. **`setCredentials` merges** (`{...prev, ...values}`), not wholesale replace — rotating one secret never drops the others (the wizard always sets the provider's full field set, so merge = replace for it; merge is the safe general default). **`deleteCredentials` is idempotent** — `Object.hasOwn(map, id)` short-circuits to a no-op (no file touch) when the id is absent, so cascading a channel removal that never had secrets is safe; deleting the last entry leaves an empty `{}` map. **Whole-file validation** (`isCredentialsMap`: plain object, not an array, every value a string bag → else `{}`, mirroring `normalizeSettings`'s whole-doc granularity so a partially-malformed file is treated as corrupt, not partially trusted); the per-id lookup/delete uses **`Object.hasOwn`** to dodge the `toString`/`constructor` Object.prototype-key pitfall (real credIds are UUIDs so it never bites, but it's honest — a `getCredentials("toString")` test pins it). **Distinct file** from spec-11 `credential.json` (singular — the bearer token) and `config.json` (settings); all three coexist under `~/.openhammer/`. Tests prove the isolation the spec names: a seeded `config.json` is byte-identical after credential ops (`saveSettings` round-trips unchanged), a decoy id-keyed value in `config.json` isn't read by `getCredentials`, and `readdirSync` shows both files coexisting; owner-only perms (`0600`/`0700`) + atomicity (no `.tmp` leftovers, overwrite) are asserted verbatim from `settings.test.ts`. **Export surface:** only `CredentialValues` (the per-id bag type, in the signatures) + `credentialsPath`; the whole-map `CredentialsMap` type is module-private (no external consumer needs it; `doctor` 17p reads perms via `credentialsPath`, not the map).
- [x] **17f.** `src/config.ts` (extend) — add `resolveConfig(args, env, settings): Config` following Node.js CLI best-practices §3.4 precedence: **CLI flags > env > local/project config > user config** (concretely `--channel <id>` > `MCP_*`/`NGROK_AUTHTOKEN`/`MCP_ALLOWED_CLIENTS` > `~/.openhammer` `Settings`); env still wins over file (backward compatible), `loadConfig(env)` (spec 01) unchanged. + tests (flags > env > file; file fills the gaps). *deps: 17d.* **No breaking change for operators using env today.** **Deviation recorded:** the merge result rides on a **`ResolvedConfig extends Config`** subtype, **not** a mutation of the base `Config`. Adding required fields to `Config` would have broken 4 minimal-`Config` test helpers (`server`/`middleware`/`token`/`http-transport` `.test.ts` — each annotates `: Config` and returns a full 6-field literal) **and** forced edits to the spec-01 `loadConfig` `toEqual` blocks; the subtype keeps `Config` + `loadConfig` **byte-identical** (spec-01 tests literally stay green — verified), and since `ResolvedConfig` is assignable to `Config`, `buildFastify(config)`/`ensureToken(config)`/etc. consume it unchanged. Single-source-of-truth honored — it's composition (a superset), not a parallel/migration type: `resolveConfig` produces it directly, no adapter. The §3.4 merge is **two doc-resident families** layered on the unchanged env `loadConfig` server shape: **channel selection** (`args.channel` (`--channel`) > `settings.defaultChannel`, `null` = localhost-only) and the **MCP client allowlist** (`MCP_ALLOWED_CLIENTS` env > `settings.mcp.allowedClients`, `[]` = any). `MCP_ALLOWED_CLIENTS` is parsed **in `resolveConfig`, not `loadConfig`** — the env-vs-file merge needs the **raw env presence check**; an empty parsed array is truthy and can't signal "env unset → use file", so `loadConfig` stays the env-only layer for the 6 spec-01 vars (it returns the unchanged server shape; `resolveConfig` reads `env.MCP_ALLOWED_CLIENTS` directly, narrowed via `!== undefined && .trim() !== ""` → **zero `as`**). Empty/whitespace `MCP_ALLOWED_CLIENTS` is treated as unset (falls through to the file), matching `loadConfig`'s empty-env posture (`MCP_AUTH_TOKEN || undefined`). **`NGROK_AUTHTOKEN` is deliberately NOT a `Config` field** — it's a *secret*, and secrets live in `credentials.json` (not the settings doc, per 17e); its env override is resolved at provider time (17i/17q), not here. The §3.4 multi-source merge applies only to values present in >1 source, so the two non-secret, doc-resident families above are the complete merge surface (the concretely-clause `NGROK_AUTHTOKEN` is an env *override* for a credential, not a config-precedence candidate). `args: CliArgs` (exported, `{ channel?: string }`) is the minimal flag subset `resolveConfig` needs today; 17n's full `parseArgs` shape will satisfy it (17o passes the parsed flags in). Return is annotated `: ResolvedConfig` (a `Config`) — the task's literal `: Config` is the consumer-facing contract; the subtype is the true shape exposing the merged fields to 17q (`channelId`) and 17r (`allowedClients`). Tests: 7 — flags > env > settings precedence, file-fills-gaps, null-channelId localhost-only, empty-env→file fallthrough, list parsing (comma/newline/trim/drop-empty), env server-shape preserved, merged-fields-on-full-Config; trio green (331 passed / 1 skipped).
- [x] **17g.** `src/tunnel/types.ts` (`ChannelHandle { url; stop?(): Promise<void> }`, `ChannelProvider { kind; mode: "live"|"static"; fields: ConfigField[]; isAvailable; probe?; start?; resolve? }`; re-export `ConfigField`) + `src/tunnel/index.ts` (`CHANNELS: Record<ChannelKind, ChannelProvider>` + `getChannel(kind)`). + test (register a fake channel, resolve by `kind`; live has `start`, static has `resolve`). *deps: 17c, 17d.* **The registry: adding a channel is one file + one registry line — no caller or wizard changes (the scalability payoff).** **Deviation recorded:** `ChannelKind`/`ChannelMode`/`ChannelEntry` are **imported from `src/config/settings.ts` (17d), not redefined** — they're the const-object + derived-union single source the loader and registry share (re-exporting them from `types.ts` would fork one). `types.ts` only defines the registry vocabulary (`ChannelHandle`/`ChannelProvider`) and re-exports `ConfigField` (so a provider imports its field schema from the registry layer alone); `ChannelProvider.{kind,mode,fields,isAvailable,probe?,start?,resolve?}` matches spec 17 lines 90-99 verbatim (`start?: Promise<ChannelHandle|null>`, `resolve?: ChannelHandle | null`, `probe?: Promise<Result<void,Error>>`). **The registry is `Partial<Record<ChannelKind, ChannelProvider>>`, not the spec's full `Record`** — no providers exist in 17g (17h cloudflare / 17i ngrok / 17j static land them), so a full `Record` would lie (and with `noUncheckedIndexedAccess` *off*, a full-`Record` index hides the absent-kind case as `ChannelProvider`); `Partial` makes `getChannel → ChannelProvider | undefined` type-honest at runtime and stays correct once providers land (undefined-for-unknown-kind is the graceful path, not a defect). Mutation is the single exported `registerChannel(provider)` (`CHANNELS[kind] = provider`, last-wins) + `unregisterChannel(kind)` (test hygiene) + `getChannel(kind)`; `index.ts` re-exports the types. **No provider self-registration** — that pattern needs a side-effect `import "./providers/x.ts"` in `index.ts`, and ESM hoists imports above the module's top-level `const registry`, so a provider's bottom-of-file `registerChannel(...)` would hit the registry's TDZ (the classic ESM-cycle trap); instead each provider will `export const xProvider` and 17h/i/j add `import { xProvider }` + a `registerChannel(xProvider)` line to `index.ts` (the "one registry line", no cycle). **8 tests** (`src/tunnel/index.test.ts`): unregistered→undefined, live registers+resolves (start present, resolve absent), static registers+resolves (resolve present, start absent), `CHANNELS` keying, last-wins overwrite, call-through to `isAvailable`/`start`/`resolve` (incl. static `resolve({})→null` graceful-absent), unregister removes, unregister-of-unknown is a no-op; `afterEach` restores the registry (fakes use real `ChannelKind`s); trio green (339 passed / 1 skipped).
- [x] **17h.** `src/tunnel/providers/cloudflare.ts` — **live** channel wrapping spec-13 `startTunnel`: `mode:"live"`, `fields: []` (zero-account quick-tunnel); `isAvailable` = `isToolAvailable("cloudflared")`; `start` maps `{ url, child }` → `{ url, stop: async () => child.kill() }`; `null` on absent binary (unchanged graceful fallback). + tests (presence/absent branches, reusing the `cloudflare.test.ts` fake-spawn shape). *deps: 17g, 13.* **Deviation recorded:** the provider is built by `createCloudflareProvider(deps?): ChannelProvider` with the production export `cloudflareProvider = createCloudflareProvider()` — the `11a`/`13`/`17d` injection-arg precedent, so the registry's `ChannelProvider` shape stays clean while tests inject `isAvailable`/`spawn` (the `spawn` dep threads `cloudflare.test.ts`'s `fakeCloudflared` through the **real** `startTunnel` plumbing — no parallel mock). One injected `isAvailable` feeds **both** the provider's presence check and the `startTunnel` call (single presence source — they can't disagree). The spec's literal `stop: async () => child.kill()` gains a `!child.killed` guard → idempotent teardown (a double `stop()` / double signal is a no-op, matching 14b's `shuttingDown` posture); the guard is the only deviation from the verbatim spec text. Wired into the registry as the "one registry line": `index.ts` does `import { cloudflareProvider }` + `registerChannel(cloudflareProvider)` declared **after** `const CHANNELS = {}` (no TDZ — the provider module only exports a const, never self-registers, avoiding the ESM-cycle trap the 17g note flags). `src/tunnel/index.test.ts` updated for the now-pre-populated registry: the "returns undefined for an unregistered kind" test uses `ngrok` (cloudflare is no longer unregistered), and a new first test asserts `getChannel("cloudflare")` is the real provider (must run before the file's `afterEach` unregisters it). 7 provider tests (shape, presence present/absent, `start → null` absent + PATH-stripped-default, `{url,stop}` happy path, `stop` kills the child via the shared `spawned[]` ref, idempotent `stop`); trio green (347 passed / 1 skipped).
- [x] **17i.** `src/tunnel/providers/ngrok.ts` — **live** channel via the `@ngrok/ngrok` SDK: `mode:"live"`, `fields: [{ key:"authtoken", label:"ngrok authtoken", kind:"secret", required:true, help:"dashboard.ngrok.com" }]`; `isAvailable` = authtoken present; `start` = lazy `import("@ngrok/ngrok")` then `ngrok.authtoken(t)` → `const l = await ngrok.connect({ addr: port })` → `{ url: l.url(), stop: () => l.close() }` (dynamic import — boot doesn't load the native binding unless ngrok is configured); `probe` = a short `connect()` + `fetch(/health)` round-trip (under `ora`); `null` (never throw) when there is no authtoken. + tests (authtoken-absent → `null`; happy path with an injected fake `ngrok`). *deps: 17g.* **SDK over agent-binary: the URL comes back synchronously — no stdout-scraping, no fresh-subdomain DNS-provisioning flakiness (the exact failure mode that made cloudflare quick-tunnels unreliable on the dev network).** **Deviation recorded:** built by `createNgrokProvider(deps?): ChannelProvider` with the production export `ngrokProvider = createNgrokProvider()` — the `11a`/`13`/`17d`/`17h` injection-arg precedent — so `deps` (`loadModule`/`fetch`/`probePort`) lets the unit tests exercise start/probe hermetically (a recording fake `NgrokModule` + fake listener + injected `fetch` → no native binding, no network, **no `vi.mock`, no `as` casts**). `loadModule`'s default is the lazy `() => import("@ngrok/ngrok")` (contextual `Promise<NgrokModule>` return annotation, **no cast** — the real SDK namespace structurally satisfies the minimal `NgrokModule`/`NgrokListener` slices, connect-param bivariance absorbs the SDK's wider `Config|string|number`), so the binding loads only when `start`/`probe` run **with an authtoken present** (the spec's "never loads at boot unless configured"). `start(localPort, options)`: no authtoken → `null` **before** loading the binding (the graceful-absent case); any failure (import/authtoken/connect rejection, `url()===null`) → `null`, **never throws** (boot continues localhost-only — distinct from `probe`, which surfaces a bad authtoken); `stop` is idempotent (`stopped` flag, mirroring 14b's one-shot shutdown) + best-effort `.catch(()=>{})` close. `probe(options)`: connect rejection → `err` (the authtoken-validation signal — `start` would silently fall back; `probe` must tell the operator before the channel is persisted); `/health` fetch `ok`→`ok(undefined)` else `err`; the listener is closed in a `finally` win-or-lose. **Two intentional deviations from the spec line:** (1) **`ora` is NOT imported here** — it is a `devDependency` (CLI-only, §2.1 footprint) while this module runs in the **prod server** (boot calls `start`); importing it would crash the `npm ci --omit=dev` image. The spinner is a caller/wizard concern (17k wraps the `probe()` call in `ora`), so the provider is a pure `Result`-returning function. (2) the probe's local port comes from `deps.probePort`, **not** the `probe(options)` signature (which carries only field answers — no port); main.ts (17q) / the wizard (17k) create a configured provider with `probePort` set, while the bare registry `ngrokProvider` is for listing/field-discovery + `start` (which receives `localPort` directly) — its `probe` errs on the missing port until wired (reconciled when 17k/17q land). Registry: `index.ts` adds `import { ngrokProvider }` + `registerChannel(ngrokProvider)` (the "one registry line", after `CHANNELS` — no TDZ); `index.test.ts` updated — the first test now asserts **both** cloudflare + ngrok are pre-registered, `afterEach` unregisters ngrok, and the "returns undefined" test uses `nginx` (still unregistered; 17j lands nginx/static-url). 14 provider tests (shape/fields, isAvailable present+empty+whitespace, start null-no-authtoken-no-load, start happy authtoken+connect+url, start null-on-connect-reject, start null-on-null-url+close, stop closes, idempotent stop, probe err-no-authtoken-no-load, probe err-no-port, probe ok-on-200+close, probe err-on-connect-reject, probe err-on-502+close, probe err-on-fetch-throw+close); trio green (361 passed / 1 skipped).
- [x] **17j.** `src/tunnel/providers/static.ts` — **static/deployed** channels (`nginx`, `static-url`): `mode:"static"`, **no `start`** (the operator stands up the endpoint — nginx/reverse-proxy on a server — that forwards to the local port); `fields: [{ key:"publicUrl", label:"public URL", kind:"text", required:true }]` (nginx adds an optional upstream hint); `resolve(opts)` → `{ url: opts.publicUrl }` (no `stop`); `probe` = `fetch(publicUrl/health)` to confirm the proxy reaches the server. Makes "deploy to a webserver" a first-class persisted channel. + tests (resolve returns declared URL; probe pass/fail with a local stand-in). *deps: 17g.* **Deviation recorded:** one factory `createStaticProvider(kind: "nginx" | "static-url", deps?): ChannelProvider` builds **both** kinds — they differ only in field schema (`staticFields(kind)`: both collect the required `publicUrl`; `nginx` adds an optional `upstream` hint for the operator's reference), so `resolve`/`probe`/`isAvailable` are identical and parameterized by `kind`. The `11a`/`13`/`17h`/`17i` injection-arg precedent holds: `deps.fetch` lets the unit tests exercise the `/health` probe pass/fail hermetically (a recording fake fetch), while the production exports `nginxProvider`/`staticUrlProvider = createStaticProvider("…")` use `globalThis.fetch`. `resolve` is **graceful-absent** (`null` when no non-empty `publicUrl`, never a throw) — mirrors a live provider's `start`, so the boot path (17q) can continue localhost-only; it returns `{ url: opts.publicUrl }` verbatim with **no `stop`** (the operator owns the endpoint). `probe` is the wizard/doctor-time validation: no `publicUrl` → `err("publicUrl is required")` (and never fetches); `fetch` non-ok → `err("publicUrl /health returned <status>")`; fetch throw → `err("publicUrl /health failed: <msg>")`; 200 → `ok(undefined)` — surfacing whether the operator's reverse proxy actually reaches the server (spec 17's "confirm the proxy reaches the server"). `isAvailable` is the cheap "a non-empty `publicUrl` is set" check (mirrors ngrok's authtoken presence), which doctor (17p) uses for the static kind. A `healthUrl()` helper strips a trailing slash so `${publicUrl}/health` never doubles it (`//health`) — nginx collapses it, but a correct URL reads cleanly. Registry: `index.ts` adds the two `registerChannel` lines (`nginxProvider` + `staticUrlProvider`) — the "one registry line" per provider; the doc comments noting "empty until 17h/i/j populate it" are corrected to "four kinds ship with providers". `index.test.ts` updated: the first test now asserts **all four** providers (cloudflare/ngrok live, nginx/static-url static, with field counts + resolve-present/start-absent), `afterEach` unregisters `nginx` too, and the "returns undefined for an unregistered kind" test now `unregisterChannel("nginx")`s first — **all four `ChannelKind`s are registered now, so no real kind is left unregistered** to exercise the absent path with. **Zero `as` casts:** the recording fake fetch is inferred `(input: string | URL | Request) => Promise<Response>` and assignable to `typeof fetch` (fewer-params-OK, the ngrok precedent), not cast — note the project's lib exposes no DOM `RequestInfo` alias, so `string | URL | Request` (undici's fetch-input shape) is used instead. 11 tests (shape per kind, isAvailable presence, resolve happy + graceful-absent, probe no-publicUrl/ok-200/trailing-slash-strip/non-ok/throw, production-export sanity); trio green (372 passed / 1 skipped).
- [x] **17k.** `src/tui/wizards/channel.ts` — the channel-add wizard: select a `ChannelProvider` from `CHANNELS` → `runWizard(provider.fields)` → `provider.probe?.(answers)` validate (under `ora`) → on success append a `ChannelEntry` to `Settings` + `setCredentials(id, …)` + set `defaultChannel` if first. Driven entirely by registry + schema, so a new channel needs **zero wizard edits**. + tests (happy path with injected io + a fake provider; cancel → no write; probe-fail → no write). *deps: 17c, 17g, 17d, 17e.* **Deviation recorded:** the seam is `addChannel(settings, deps: AddChannelDeps = {}): Promise<Result<Settings, Error> | null>` — it persists **secrets** (`setSecrets` → `setCredentials`) but returns the updated `Settings` for the CLI to `saveSettings`, mirroring 17m's remove/use split (the CLI owns the settings-doc write; the wizard owns the secret write + the in-memory entry append). The result spine is the graceful-absent vs surfaced-failure split the TUI layer uses: `ok(Settings)` (added — entry appended, `defaultChannel` set when it was the first channel, secrets written when any); `err(Error)` (a static provider's probe failed — no write, the CLI reports `message`); `null` (cancel / required-empty — no write, silent). All five moving parts are injected (the `11a`/`13`/`17b`–`17j` precedent): `io` (select + field prompts, defaults to `defaultIo`), `channels` (defaults to `Object.values(CHANNELS).filter(...)` **read at call time** — tests mutate `CHANNELS`, so never snapshot at module load), `newId` (defaults to `crypto.randomUUID`), `setSecrets` (defaults to `setCredentials`), and `probeRunner` (defaults to a lazy-`import("ora")` spinner that `.succeed()`/`.fail(message)`s — `ora` is a devDep and the lazy import keeps it out of the test graph; tests pass an identity `(_label, fn) => fn()`). **Probe gate — static only at add-time (the 17i reconciliation).** The 17i note's "reconciled when 17k lands": the bare-registry `ngrokProvider`'s probe needs a local-server port absent at `channel add` time, so the wizard probes **only `mode === "static"` providers** (whose endpoint — the operator's reverse proxy — is already deployed, so `fetch(publicUrl/health)` is a real reachability check). A `mode === "live"` provider's endpoint is created by OpenHammer at `start` time and doesn't exist at config time, so its probe is deferred to runtime (`start`/`doctor`, 17q/17p) — the gate is a `mode` rule (no per-channel code), and `channel add` works for all four kinds (cloudflare: live, no probe, no fields → empty-options entry, no secrets written; ngrok: live, probe skipped → authtoken persisted; nginx/static-url: static, probed). **Composition — one banner, from `runWizard`:** the provider `io.select` leads, then `runWizard` frames the field phase with the banner + `intro`/`outro` (the wizard prints no banner of its own — that would double it; this mirrors 17l's section-wizard pattern, so both families reuse `runWizard`'s framing). **Secret split** iterates `provider.fields`: `kind === "secret"` → the credentials bag, everything else → the entry's `options` (a `confirm` lands as `"true"`/`"false"`, a `select` as its value); `setSecrets` is called **only when the bag is non-empty** (a zero-secret channel never touches `credentials.json`); the probe's local is captured in a `const probe = provider.probe` so the closure narrows with **no `!`**. 7 tests (static add + probe + defaultChannel-first + banner; live add + secret/options split + probe-skipped; defaultChannel-unchanged-when-not-first; cancel-select; cancel-field; probe-fail-static-err; empty-registry-err); trio green (379 passed / 1 skipped).
- [x] **17l.** `src/config/sections.ts` (`ConfigSection { id; label; fields; read(s); write(s, vals) }` + `CONFIG_SECTIONS: { mcp }`) + `src/tui/wizards/section.ts` — the settings-section wizard (`config set`): seeds `runWizard` from `section.read(settings)` → `section.write(...)` → `saveSettings`. The `mcp` section edits `allowedClients` (list → `string[]`). Same wizard engine as channels — proves the scalability seam. + tests (section round-trip; adding a fake section needs no wizard change). *deps: 17c, 17d.* **Deviation recorded:** the `mcp` section's list parsing reuses **`parseClientList`, now exported from `src/config.ts`** (17f) rather than re-implementing the comma/newline/trim/drop-empty contract — the single source of the `allowedClients` format shared by `MCP_ALLOWED_CLIENTS` (env, `resolveConfig`) and the section field (so the two paths can never drift); its doc comment is broadened from "`MCP_ALLOWED_CLIENTS` env value" to the shared "client-name list from a comma/newline-separated string". `mcpSection.read(s)` joins the current `string[]` with `", "` for display, `write` re-parses — comma-joined round-trips through parse (verified). The read→seed seam is a pure exported `seedDefaults(fields, seed): ConfigField[]` in `section.ts` (mirrors `reduceFields` being exported in `wizard.ts` — the part worth unit-testing without a terminal): it overrides each field's `default` from the seed, coercing a `confirm` field's `"true"`/`"false"` seed back to a boolean (mirroring how `reduceFields` coerces confirms to those strings) and leaving a field unchanged when the seed omits its key — the `if (field.kind === "confirm") … else text|secret|select` shape copied from `reduceFields`. **Result spine mirrors 17k exactly:** `ok(Settings)` (section applied — `section.write` over the answers; the CLI `saveSettings`s the returned doc — the same doc-via-caller split, **minus the secret half** since a section holds no secrets), `err(Error)` (no sections registered — defensive; `mcp` always ships, so unreachable in prod), `null` (cancel / required-empty — no write). **Composition — one banner, from `runWizard`:** the section `io.select` leads, then `runWizard` frames the field-editing phase with the banner + `intro`/`outro` (the wizard prints no banner of its own — that would double it; the 17k pattern). All seams injected (`io`, `sections` read at call time, `stream`) — the `11a`/`13`/`17b`–`17k` precedent — so clack never touches the hermetic trio. **Scalability seam proven by test:** `setSection` drives a foreign fake section (a `confirm` + `text` field) through `read → seed → write` unchanged — adding a section is one registry entry, zero wizard edits. No cycle: `sections.ts` → `config.ts` (value) + `settings.ts` (type); `config.ts` does **not** import `sections.ts`. 26 new tests (16 `sections.test.ts` + 10 `section.test.ts`); trio green (405 passed / 1 skipped).
- [x] **17m.** `src/tunnel/manage.ts` — pure `Result` ops over `Settings`: `listChannels`, `removeChannel(settings, id)` (also `deleteCredentials`), `setDefaultChannel(settings, id)`. The CLI calls these then `saveSettings`. + tests (id-not-found → `err`; remove cascades its credentials). *deps: 17d, 17e.* **Deviation recorded:** the three ops split cleanly along their failure modes — `listChannels(settings): ChannelEntry[]` is a pure read with no failure mode, so it carries **no `Result`** (returns a shallow copy `[...settings.channels]` so a caller can't mutate the doc's array through the result); `removeChannel`/`setDefaultChannel` return `Result<Settings, Error>` — `ok(Settings)` (a new doc for the CLI to `saveSettings`), `err(Error)` on an unknown id (nothing written). **Boundary posture mirrors `addChannel` (17k) exactly** — the inverse of its `setCredentials`: `removeChannel` cascades the channel's secret teardown *inside* the op (`deleteCredentials(id)`, the throwing persistence boundary → propagates to the CLI as actionable stderr), while the settings doc is returned for the caller to save. The `Result` spine is reserved for the expected *domain* failure (unknown id); the credential write's throw is the infrastructure failure that the CLI boundary handles — same split as `addChannel`'s `setSecrets`. `deleteCredentials` is idempotent (a no-op when the channel never had secrets — e.g. zero-field cloudflare), so the cascade is always safe. **Default-channel invariant:** `setDefaultChannel` only points at an *existing* channel (unknown id → `err`), so the default can never dangle; `removeChannel` keeps that invariant by resetting `defaultChannel` to `null` when the removed channel *was* the default (the operator re-picks with `use`), never leaving a stale id. Treats `settings` as readonly — every op returns a new object (spread + filtered array), verified by no-mutation tests. The cascade is injected via `ManageDeps { deleteCreds?: (id: string) => void }` defaulting to `deleteCredentials` (the `11a`/`13`/`17b`–`17k` injection-arg precedent) — a single-arg signature so a unit test injects either a recording fake (asserts the cascade fires + that the unknown-id path cascades nothing) **or** the real `deleteCredentials` bound to a temp `credPath` (asserts the real secret teardown against a temp `~/.openhammer/credentials.json`, the strongest proof "remove cascades its credentials" — and that a sibling channel's secrets survive, and that the no-secrets channel is safe). 15 tests, trio green (420 passed / 1 skipped).
- [x] **17n.** `src/cli.ts` (`bin: openhammer` → `dist/cli.js`) + `src/cli/args.ts` — `parseArgs(argv): { command, rest, diagnostics: { type: "warning" | "error"; message }[] }` (pi's args shape — an unknown option pushes a diagnostic, never throws) + `printBanner` on interactive launch. + tests (dispatch table, unknown-option → diagnostic). *deps: none (pure parsing).* **Deviation recorded:** the atomic split this checkbox encodes — **17n ships the parse + interactive-banner seam; 17o wires the command dispatcher** (`channel`/`config`/`doctor`/`monitor`/`start`→boot). `src/cli.ts` exports `runCli(argv, deps: CliDeps = {}): ParsedArgs` (parse → print banner when interactive → write diagnostics to stderr → return the parsed args); the module-level auto-run guard (`pathToFileURL(process.argv[1]).href === import.meta.url`, the T-canary pattern — inert under vitest) calls `runCli(process.argv.slice(2))`, and 17o replaces that tail with `const parsed = runCli(…); await dispatch(parsed)` with **zero** re-parsing. `*deps: none*` is honored literally: `args.ts` imports nothing, and `cli.ts` imports only `parseArgs` + `printBanner` — no `main`/wizards/`manage` (those land in 17o). **`parseArgs` is pure and total** — every input yields a result; problems land in `diagnostics`, never thrown (pi fidelity: `Diagnostic = { type: "warning" | "error"; message }`). It knows OpenHammer's flags (`--tunnel`, `--channel <id>` incl. the `--channel=<id>` equals form, `--help`/`-h`) but **deliberately does NOT validate the command set** — `command: string | null` is the raw first positional (the dispatcher owns the string→handler table, so adding a command needs no parser edit); an unrecognized command word is left for 17o, not flagged here. Only *options* become diagnostics: unknown `--long`→`warning`, unknown short→`error` (pi's severity split), and `--channel` with no value (or whose next token is dash-leading, i.e. another flag)→`error` — the value heuristic uses an `i+1 < argv.length` bounds check (`noUncheckedIndexedAccess` is off, so the in-bounds index reads as `string` with no `as`). `rest` carries every positional after `command` (subcommand + operands), flags parsed out. **Banner-on-interactive is the tested runtime behavior:** `runCli`'s `CliDeps { stdout?; stderr?; isTTY? }` routes side effects through injectable `BannerStream` fakes (a recording write sink), so the 4 `cli.test.ts` cases assert banner-on-TTY / no-banner-off-TTY / diagnostics-to-stderr / parsed-args-return **without touching the real process streams**; `isTTY ?? process.stdout.isTTY === true` respects an explicit `false` (`??` only falls through on nullish). **Build verified:** `npm run build` emits `dist/cli.js` (shebang `#!/usr/bin/env node` preserved by `tsc`) + `dist/cli/args.js`, and the chmod step makes it `-rwxr-xr-x`; `npm start` (`dist/main.js`, spec 14) is unchanged — the bin is the operator-facing entry 17o grows into. Runtime smoke confirmed: `node dist/cli.js --bogus`→`warning: Unknown option: --bogus` on stderr / exit 0 / no banner (non-TTY); `channel list`→silent parse / exit 0; an interactive launch prints the README banner. 23 new tests (19 `args.test.ts` + 4 `cli.test.ts`); trio green (443 passed / 1 skipped).
- [x] **17o.** subcommands wired in `src/cli.ts`: `openhammer channel { add | list | remove <id> | use <id> }`, `openhammer config { get | set [section] }` (default section `mcp`), `openhammer doctor`, `openhammer monitor`, default **or** `openhammer start` → delegate to the spec-14 `main()` boot. + tests (arg → command mapping; `list`/`remove`/`use` against a temp `HOME` settings dir). *deps: 17n, 17k, 17m, 17l, 14b.* **Deviation recorded:** `dispatch(parsed, deps): Promise<DispatchOutcome>` (a `number | undefined`) is the command registry — each `case` is its entry, so the families whose deps are satisfied ship now (`channel`/`config`/`start`/default) and `doctor`/`monitor` land their `case` + impl in their **own** iterations (17p diagnostics registry, 17t monitor socket). Wiring them here would require stubbing those not-yet-existing modules — forbidden by the no-stub guardrail; an as-yet-unwired command prints usage + exits `2` (a real, complete behavior — the dispatcher genuinely doesn't know that command yet), and `--help` lists `doctor`/`monitor` as the documented surface so operators see them coming. **`start`/default delegates to `main()`**, so `src/main.ts` is now `export async function main()` with its top-level `main().catch(...)` auto-run **guarded** by the `pathToFileURL(process.argv[1]).href === import.meta.url` check (the cli.ts/T-canary pattern) — `npm start` (`dist/main.js`) and the boot E2E (`tsx src/main.ts`) still fire it verbatim (re-verified: 4 passed / 1 skipped), but importing `main` from the CLI (or a test) does **not** boot. `defaultBoot` lazy-`import("./main.ts")`s so `channel list` etc. never load the server stack. **Boot-delegation bug found + fixed (the integration regression this careful smoke catches):** `main()` *resolves* once setup is done — Fastify's listening socket is what keeps a standalone `dist/main.js` alive. The first cut's auto-run did `dispatch(parsed).then(code => process.exit(code))`, so after boot resolved it `process.exit`-ed and **killed the server the instant the banner printed** (confirmed by smoke: token printed, process dead, `/health` unreachable). Fix: `dispatch` returns `undefined` for `start`/default ("server running") and the auto-run only `process.exit`s on a `number` — so the event loop (Fastify) keeps the process alive until `main`'s `SIGINT`/`SIGTERM` handlers exit `0`. Re-smoked: `openhammer start` → `/health` 200 → process alive → clean SIGTERM exit 0. The two boot routing tests assert `code === undefined` + the injected fake boot was called (no real server). **Banner dedup:** the wizard commands (`channel add`/`config set`) frame themselves with the banner via `runWizard`→`withSession`, but `runCli` (17n) already printed the session banner on a TTY launch — so the dispatcher passes the wizards a `silentStream` (`{ write: () => false }`, a `BannerStream`) so exactly one banner prints instead of two (the `clack` `intro`/`outro` still frame the field phase). `config set [section]` validates the section arg against `CONFIG_SECTIONS` (only `mcp` ships) before running `setSection` (which still picks among registered sections — trivial with one section; preselecting a named section is a future `setSection` enhancement). Exit-code contract: `0` success, `1` handled domain failure (`remove`/`use` of an unknown id → the manage ops' `err`), `2` usage error (unknown/missing subcommand, unknown config section). 19 tests (7 routing with injected handlers + 12 real-handler e2e against an isolated `HOME` via a `withTempHome` helper that sets `process.env.HOME` to a temp dir, seeds `~/.openhammer/config.json`, and restores/cleans up); trio green (462 passed / 1 skipped).
- [x] **17p.** `src/diagnostics/registry.ts` (`DiagnosticCheck { id; run(): Promise<{ status: "pass" | "warn" | "fail"; message }> }`) + `src/cli/doctor.ts` — checks: `config.json` parses; each `Settings.channels` provider `isAvailable` (live) or `probe`-reachable (static); `credentials.json` perms `0600`; `rg`/`fd` present. `openhammer doctor` runs all, prints grouped by status. + tests (each check pass/warn/fail; run-all ordering). *deps: 17n, 17g, 17d.* **Deviation recorded:** **`registry.ts` is pure vocabulary + an array container** (`DIAGNOSTICS: DiagnosticCheck[]`, `registerDiagnostic`/`unregisterDiagnostic`/`getDiagnostics`) — imports nothing (parity with `result.ts`), so importing it alone yields an empty registry and the unit tests register fakes directly. The container is an **array, not a `Record` keyed by id** — `doctor` prints results grouped by status and run-all *ordering* is meaningful (static checks in registration order, then the dynamic per-channel checks), so insertion order is a first-class property a `Record` would demote; `registerDiagnostic` replaces by id (last-wins, preserving first-insert position — the array analog of the channel registry's keyed overwrite) and `getDiagnostics` returns a **copy** (a caller can't mutate the registry through the result). **`doctor.ts` self-registers the built-ins at load** (`createDefaultChecks().forEach(registerDiagnostic)`) — this is the **TDZ-safe shape, not the 17g trap**: `doctor.ts → registry.ts` is one-directional (registry.ts imports nothing), so registry.ts fully initializes `DIAGNOSTICS` before doctor.ts's top-level code runs; there is no side-effect `import` hoisted above a `const` in the *same* module (the 17g hazard). **Built-in checks** are factories (`createConfigCheck`/`createCredentialsCheck`/`createBinaryCheck`/`createChannelCheck`) taking injectable paths/`isAvailable`/deps (the `11a`/`13`/`17b`–`17m` injection-arg precedent) so each is unit-tested hermetically; `createDefaultChecks` builds the canonical `[config, credentials, rg, fd]` set. **The config check reuses `normalizeSettings`** — exported from `src/config/settings.ts` (a one-word `export` added to the existing private validator; additive, in-scope — it's the single-source corruption detector the check needs, so doctor flags a structurally-invalid doc that `loadSettings` silently falls back from, not just unparseable JSON). **Channel checks are dynamic** — generated from `settings.channels` at `runDiagnostics` time (runtime data, not registrable at load), appended after the static checks; the full options bag merges `entry.options` with `getCredentials(id)` so a secret-gated `isAvailable` (ngrok authtoken) sees its value, and the check branches on `provider.mode` (live → `isAvailable`, static → `probe`) since the provider owns the live/static method split. **Statuses:** `not available`/unreachable → `warn` (the channel won't function but the server boots localhost-only); an unregistered kind → `fail` (config error); `credentials.json` too-open perms → `warn` (advisory — the install still works, fix with `chmod 600`); `rg`/`fd` absent → `fail`. **`runDiagnostics(deps)`** defaults its static checks to `getDiagnostics()` (the registry is the production source-of-truth, so future plugins extend doctor by registering) + the dynamic channel checks; tests inject `deps.checks` for hermeticity. **Exit code:** `1` if any check `fail`s (lets CI/scripts gate on doctor), `0` otherwise — a `warn` is advisory and does not fail. **`doctorCommand(io, deps)`** takes a `DoctorIo = { stdout: BannerStream }` structural slice (not the CLI's `CommandIo`) to avoid a `cli.ts` import cycle (cli.ts imports `doctorCommand`; doctor.ts must not import back — even type-only); `formatDoctor(reports)` is the pure grouped-printer (fail → warn → pass, summary-led) unit-tested without an io stream; `safeRun` isolates each check (a throw → `fail`, never aborts the run). **Wired into `dispatch`** (`case "doctor": return doctor(io)` + a `doctor?: (io) => Promise<number>` injection seam in `DispatchDeps`, mirroring the `channel`/`config` seams) — the 17o "as-yet-unwired → exit 2" placeholder for doctor is replaced; `monitor` remains the sole unwired command (17t). Tests: 36 new (7 `registry.test.ts` + 25 `doctor.test.ts` + 2 `cli.test.ts`: routing + a real-handler test against an isolated `HOME`); trio green (498 passed / 1 skipped). **Smoke-verified:** `openhammer doctor` (real `dist/cli.js`) → 4 pass / exit 0 on a clean `HOME`; corrupt `config.json` → `[fail]` + `[warn]` (open creds) / exit 1.
- [x] **17q.** `src/main.ts` — `--channel <id>`/`defaultChannel` resolves the provider via the registry: **live** → `start()` (spawn); **static** → `resolve()` (print the declared URL); `null` → the unchanged localhost-only warn. Env (`NGROK_AUTHTOKEN`/`MCP_TUNNEL_PROVIDER`) kept as override. + boot-E2E additions for the registry path. *deps: 17g, 17f, 14b.* **Deviation recorded:** the resolution is factored into a testable seam — **`src/tunnel/boot.ts`** (`selectChannel` + `resolveChannelHandle`) — rather than inlined in `main.ts`, mirroring the 14a `formatStartup` / 17k `addChannel` extract-the-testable-part precedent (main.ts has process/signal concerns that make it spawn-only testable; the precedence + dispatch are unit-level). `main.ts` now: `parseArgs(process.argv.slice(2))` (re-parsed because `npm start`/`node dist/main.js` bypass the CLI dispatcher — `--channel`/`--tunnel` come straight from argv) → `loadSettings()` → **`resolveConfig({channel}, env, settings)`** (switching from `loadConfig` to the 17f merge, so `channelId` = `--channel` > `settings.defaultChannel`; the env server-shape + `allowedClients` are carried for 17r) → `ensureToken` → `buildFastify` → `listen` → `resolveChannelHandle(...)` → warn on `notice` → `printStartup({tunnelUrl: handle?.url})`. **Precedence (`selectChannel`, the §3.4 flag > env > file ordering + the legacy flag):** (1) `channelId` (the `--channel` flag *or* `settings.defaultChannel`, already folded together by `resolveConfig`) → the persisted entry, options merged as `{...entry.options, ...getCredentials(id)}` (secrets win, as in doctor 17p); `not-found` when the id names no entry; (2) `MCP_TUNNEL_PROVIDER` (env) → that provider kind, started with `options.authtoken = NGROK_AUTHTOKEN` when present (the 17f/17i "secret resolved at provider time" reconciliation — lets a CI deploy boot a tunnel with no settings doc); `unknown-provider` when the value isn't a known kind; (3) the legacy `--tunnel` flag → cloudflare (backward compat); (4) `none` → silent localhost-only. The outcome is a **`ChannelSelection` discriminated union** (`selected | not-found | unknown-provider | none`) — the mutually-exclusive-states rule — so every path is type-honest and unit-testable without a provider. `resolveChannelHandle` dispatches `selected` → live `provider.start?.(localPort, options)` / static `provider.resolve?.(options)` via the injected registry lookup; **every** non-success path is `null` + a human `notice` (never throws — the spec-13 graceful-absent posture extended to all channels: not-found / unknown-provider / unregistered kind / null start-or-resolve). **Null-safe boot:** `main.ts` logs the `notice` via `fastify.log.warn` (pino → stdout) and continues serving — the bearer gate is the real gate; a missing channel is graceful degradation, never a boot failure. The `notice` text keeps the `continuing localhost-only` substring the `--tunnel` boot E2E asserts. **Shutdown** swaps `tunnel?.child.kill()` for the channel's own teardown: `handle?.stop?.()` is best-effort (the `12b` per-promise `.catch(()=>{})` idiom, never blocks exit) — live channels kill their child, static channels have no `stop` (the operator owns the endpoint). Every dep injectable (`getChannel`/`getCredentials`) — the `11a`/`13`/`17b`–`17m` injection-arg precedent — so `boot.test.ts` (20 tests) exercises precedence + dispatch with a fake provider, no binary/network/spawn. **No `as`:** `MCP_TUNNEL_PROVIDER` is narrowed to `ChannelKind` via a local `isChannelKind(v: string): v is ChannelKind` type guard (mirrors the loader's private one in `settings.ts`) rather than a cast. **Boot E2E addition:** one deterministic test seeds a `static-url` channel as `defaultChannel` in the temp `HOME` config.json, boots `tsx src/main.ts`, and asserts the banner prints `Tunnel URL: <publicUrl>/mcp` + clean SIGTERM exit 0 — the hermetic proof that `main.ts` funnels `--channel`/`defaultChannel` through the registry (a static channel's `resolve` is pure: no spawn, no network). A live channel's real round-trip stays gated (`T-tunnel-e2e` cloudflare / `T-ngrok-channel-e2e`). **Env note:** `cloudflared` is now **present** on the dev box, so the `--tunnel` boot E2E's `skipIf(isToolAvailable("cloudflared"))` now *skips* (the live cloudflared child is non-deterministic in the hermetic trio) — the static-channel test is unaffected (no binary). Trio green (519 passed / 1 skipped).
- [x] **17r.** `src/auth/middleware.ts` (extend) — when `settings.mcp.allowedClients` is non-empty and not `["*"]`, the bearer gate also checks the inbound **`User-Agent`** against the allowlist → `403` on miss (`User-Agent` is the only per-request signal in stateless mode — `initialize`'s `clientInfo.name` isn't retained without sessions, deferred spec 18; the bearer token stays the real gate). Default `[]`/`["*"]` = any → **non-breaking**. + tests (allowed/denied/any). *deps: 17l (mcp section), 11b.* **Deviation recorded:** `allowedClients` lives on `ResolvedConfig` (17f kept `Config` minimal so its 6-field test literals stay intact), so it is threaded as an **optional 3rd param defaulting to `[]`** — `createAuthMiddleware(token, config, allowedClients = [])` → `McpHttpRoutesOptions.allowedClients?` (destructured `= []`) → `buildFastify(config, token, allowedClients = [])` → `main.ts` passes `config.allowedClients` (a `ResolvedConfig` there). Every default is `[]` = any client, so all existing 2-arg call sites (`middleware.test.ts`, `http-transport.test.ts`, `server.test.ts`, `mcp.e2e.test.ts`) are unchanged and current behavior is byte-identical — the only call site that grows is `main.ts`. The pure gate is the exported `isClientAllowed(userAgent, allowedClients): boolean` (the `parseClientList`/`extractTunnelUrl` "export the testable pure part" precedent): `[]`/`["*"]` → any; otherwise a **case-insensitive substring** match of each name against the trimmed-lowercased `User-Agent` (a client named `claude-code` admits `claude-code/1.0.6`); a missing/blank header is a miss while the gate is active (unknown client). The substring match is deliberately lenient — this is a secondary, best-effort filter **behind** the real bearer gate, so version suffixes don't false-negative an honest client. **Token gate precedence is load-bearing:** the 401 (missing/wrong token) is checked first and always wins — a wrong token + disallowed UA returns **401, not 403** (the bearer is the real gate; a client that can't authenticate never reaches the allowlist). The 403 path fires only after the token passes; its body is a JSON-RPC `{ code: -32002, message: "Forbidden: …" }` envelope mirroring the `-32001` 401 body (no special header — `reply.send` auto-sets `application/json`, as the 401 path already relies on). The hook stays `async` (the 11b load-bearing Fastify-lifecycle note) and is still wired to **POST `/mcp` only** (the `preHandler` is on the POST route), so `/health`, `/.well-known/*`, and GET/DELETE `/mcp` never hit either gate — the User-Agent filter naturally scopes to the one route that carries tool calls. 11 new tests (6 `isClientAllowed` pure: empty/`["*"]` any, case-insensitive substring, multi-name any-match, miss, missing/blank deny; 5 integration via `app.inject`: allowed admit, disallowed 403, no-UA 403, wrong-token-still-401, `["*"]` non-breaking); trio green (530 passed / 1 skipped).
- [x] **17s.** `src/mcp/telemetry.ts` (non-blocking `RequestRecorder`: in-memory ring buffer + active-client set + subscribers, fed by a Fastify `onRequest`/`onResponse` hook in the transport — records `{ ts, client, method, tool?, reqBytes, resBytes, ms, status }`) + `src/observability/status-socket.ts` (a **Unix domain socket** at `~/.openhammer/openhammer.sock`, mode `0600`, local-only — dumps recent buffer then streams NDJSON live). + tests (recorder ring/subscriber; socket auth-free + local-only). *deps: 12b, 14b.* **Deviation recorded:** the recorder is a `RequestRecorder` class — a fixed-capacity ring buffer (default 1000, modular-index head/len, `recent()` oldest→newest), an active-client `Map` (`activeClients()` → call counts + lastSeen), and a `Set<RequestSubscriber>` fanned-out on each `record`. **Non-blocking** (the spec's hard rule): `record` never throws into the path — the only realistic thrower is a misbehaving subscriber, so `fan` catches each subscriber individually and drops a thrower (ring/map ops don't throw); `attachRequestRecorder`'s finalize reads only already-resolved values (`request.body`, `reply.raw.statusCode`, `hrtime`), so it has **no blanket try/catch** on the hot path (honest + avoids the empty-catch rule). The hook is `async` (the 11b load-bearing note), scoped to `POST /mcp` (url+method filter; `/health`/well-known/GET·DELETE `/mcp` aren't recorded). Pure helpers exported for unit tests (`parseRpc`/`readUserAgent`/`readContentLength`/`readClientName`/`resolveClient` — the `extractTunnelUrl` precedent). **`resBytes` is wire bytes, not a header read** — the two discoveries: (1) Node does **not** index headers passed to `writeHead(status, headerObject)` (the path the SDK's `@hono/node-server` takes for the hijacked response), so `reply.raw.getHeader("content-length")` returns `undefined` at `finish` — empirically confirmed on raw `http.Server` + Fastify; only `setHeader` is queryable, which the SDK doesn't use. (2) `reply.raw.socket` **detaches** by `finish` (reads `null` then). So `resBytes` = the captured `socket.bytesWritten` ref at `finish` minus its value at `onRequest` start — differenced so a reused keep-alive socket doesn't accumulate prior responses. That's status line + headers + body (an ~150B upper bound on the body; exact for large tool outputs), `0` when no socket was captured. **No raw-response monkey-patching** (keeps the path clean + the `no as` rule intact — wrapping `ServerResponse.write`/`end` would need an `as` for the overload signatures, rejected). `reqBytes` is the request `content-length` (Fastify's parsed header — always present, accurate). **clientInfo is under `params`** (`initialize` → `params.clientInfo.name`), not the envelope top level — `readClientName` reads it there; `resolveClient` uses it on `initialize` only, else User-Agent (stateless — clientInfo isn't retained across requests). **Status socket** (`startStatusSocket`, null-safe like `startTunnel`): a `net.createServer` whose per-connection handler (`handleConnection`) dumps `recent()` then `subscribe`s for live events; a write failure or `close`/`error` unsubscribes idempotently (`safeWrite` returns false on throw). `chmod 0600` after listen; stale-file unlink before listen; `close()` destroys clients + removes the file. `~/.openhammer` is **`mkdir`'d `0700` by `startStatusSocket`** (not just by `ensureToken`) — caught by a manual smoke: an `MCP_AUTH_TOKEN`-override boot skips `ensureToken`'s dir creation, so the socket would ENOENT and silently go `null`; the mkdir makes the inspector robust regardless of the token source. **Wiring:** `main.ts` creates one `RequestRecorder`, threads it through `buildFastify(config, token, allowedClients, recorder)` → `mcpHttpRoutes({…, recorder})` → `attachRequestRecorder` (only when present; the optional 4th param keeps every existing call site byte-identical), starts the status socket (warns + continues localhost-only on bind failure, never a boot gate), and closes both on shutdown. **17t consumes this** unchanged (`openhammer monitor` connects to the socket + renders the NDJSON). Tests: 31 (telemetry: pure helpers + recorder ring/evict/activeClients/subscriber/drop-thrower/close + a real-listening `attachRequestRecorder` round-trip asserting method/tool/client/status/reqBytes/resBytes and that `/health` is skipped; status-socket: fake-socket dump+stream+cleanup-on-write-fail/close/error, a real-Unix-socket E2E proving dump+live+0600+clean-close, and the null-safe ENOTDIR bind failure). **Smoke-verified:** the real server records a `POST /mcp` 401 (`{client:"smoke-client/9",method:"tools/call",tool:"bash",reqBytes:71,resBytes:560,ms:1,status:401}`) and streams it live to a connected socket client — one recorder shared by the hook + the socket. Trio green (561 passed / 1 skipped); `test:compose` green.
- [x] **17t.** `src/cli/monitor.ts` + `src/tui/monitor-view.ts` — `openhammer monitor` connects to the socket and prints a **live streaming feed** of clients + each `tools/call` (client, tool, bytes, ms) — ngrok-inspector style, local-only. **Streaming, not a full-screen dashboard** (no render loop → honors §2.1 footprint); a rich dashboard is a future enhancement. + tests (parses NDJSON + renders lines on a fake socket stream). *deps: 17n, 17s.* **Deviation recorded:** the pure view (`src/tui/monitor-view.ts`) — `parseEventLine` (NDJSON → `RequestEvent | null`, hand-narrowed, **zero `as`**), `isoTimeOf` (`ts.slice(11,19)` for the ISO `HH:MM:SS`, no `Date` parsing → deterministic), `formatDuration` (`<1000→ms`/`≥1000→`M.Xs``), `formatBytes` (`NB`), `formatStatusSuffix` (`""` for 0/2xx so a clean `200` matches the spec's example line exactly, the raw code for errors), `formatEvent` (`[HH:MM:SS] client  method [tool]  duration bytes [status]`), `formatClientHeader` (`active clients: a×3, b×1`), and a `MonitorState` reducer (the active-client slice the monitor derives from its stream alone — it holds no `RequestRecorder` handle) — is fully unit-tested without a socket (the `extractTunnelUrl`/`parseClientList` "export the testable pure part" precedent). The command (`src/cli/monitor.ts`) owns only the connection + line-split → render loop. **Connection seam — per-event methods, not an overloaded `on`:** `MonitorConnection { onData/onError/onClose/destroy }` so the test fake stores typed listeners with **no `as` cast** (the production `defaultConnect` adapter wraps `net.createConnection`'s `socket.on(event, cb)` into each, `void`-discarding the `this` return — the `cb` matches each `net.Socket.on` overload, no cast). `monitorCommand(io, deps)` returns `0` on a clean socket close, `1` when the socket can't be reached: the `errnoCodeOf` helper (the no-`as` `"code" in` + `typeof` `edit.ts`/`errnoCode` precedent) recognizes `ENOENT`/`ECONNREFUSED` → the actionable "status socket was not found … Is the server running? `openhammer start`" hint; any other error → its message. The feed buffers chunks and splits on `\n` (NDJSON — a line may arrive split across chunks or carry many), skipping blank/malformed lines without aborting. **Rolling header without a render loop** (the §2.1 rule): the header is reprinted only when a *new client* joins the active set (the closest "rolling" behavior achievable by append-only output) — it can't redraw in place, which is the dashboard the spec defers. **The banner is printed by `runCli` on a TTY launch** (as for `doctor`/`config`/`channel list`); `monitorCommand` does NOT self-print it (it would double) — it writes a one-line `Monitoring <path> — Ctrl+C to stop.` intro, then streams (deviation from the spec's literal "clack `intro` for the banner": honored in spirit by the intro line; the banner reaches stdout via `runCli`, and avoiding clack here keeps the long-running stream + hermetic test free of a TTY dep). **Ctrl+C** is the default SIGINT behavior (terminate, like `tail -f`) — no signal handler installed, keeping the command free of global side effects (a clean shutdown-on-signal is a future polish). **Wired into `dispatch`** (`case "monitor": return monitor(io)` + a `monitor?: (io) => Promise<number>` seam in `DispatchDeps`, mirroring `doctor`) — the 17o "as-yet-unwired → exit 2" placeholder for monitor is replaced; `parseArgs` already recognized `monitor` (17n), so the dispatcher is the only edit. Tests: 30 new (20 `monitor-view.test.ts` pure + 9 `monitor.test.ts` incl. one **real `startStatusSocket`→`monitorCommand` E2E** proving the production `defaultConnect` adapter dumps + streams live over a real local Unix socket + resolves 0 on server close, mirroring the 17s real-net precedent + 1 `cli.test.ts` routing test); trio green (591 passed / 1 skipped). **Smoke-verified:** `node dist/cli.js monitor` with no server → the actionable `status socket was not found … openhammer start` hint / exit 1.
- [x] **17u.** *(revision to 17i — shipped SDK-based)* switch `src/tunnel/providers/ngrok.ts` from `@ngrok/ngrok` to driving the **`ngrok` CLI**: `isAvailable` = `isToolAvailable("ngrok")`; `start` spawns `ngrok http <port>` (authtoken via `NGROK_AUTHTOKEN` env) and polls `GET http://127.0.0.1:4040/api/tunnels` for `tunnels[0].public_url` → `{ url, stop: () => child.kill() }`; `probe` = `fetch(url/health)`; `null` on absent binary / no URL in time. **Remove `@ngrok/ngrok` from `dependencies`** (now unused). Update `ngrok.test.ts`'s injected fake: a fake CLI spawn + `:4040` JSON instead of a fake SDK module. + tests. *deps: 17i.* **Deviation recorded:** the SDK's bundled core defaults to QUIC/UDP, which hangs on the dev network, and its JS API exposes no transport knob to flip it; the system CLI works locally and its inspector API returns the URL with no stdout-scraping (the original reason to prefer the SDK no longer holds). Tradeoff: operator needs the `ngrok` binary (presence-checked, like `cloudflared`). See `specs/17` (ngrok provider + decisions, revised). **Cleanup of the SDK mess (the bad-plan fallout):** after removing `@ngrok/ngrok`, regenerate `package-lock.json` (`npm install`) so the lockfile drops it + its transitives; then `grep -rn "@ngrok/ngrok" src test README docs` and remove any straggling references (imports, docs, the `ngrok` provider's old SDK test fakes) so nothing dead-ends on the removed dep. **Shipped:** `createNgrokProvider(deps)` keeps the injection-arg precedent — `isAvailable`/`spawn`/`fetch`/`inspectorUrl`/`timeoutMs`/`pollIntervalMs`/`probePort`; `ngrokProvider` uses the real CLI + global `fetch`. `isAvailable` is now **binary-presence only** (`isToolAvailable("ngrok")`), dropping the old authtoken gate (per the revised spec 17i — "same presence-check as cloudflare"); the authtoken is validated at `start`/`probe` (missing → `null` from `start`, `err` from `probe`), so doctor's live-channel check now reports "ngrok ready" iff the CLI is installed (doctor's per-channel comment updated to match). `start` spawns `ngrok http <port>` with the authtoken passed as **`NGROK_AUTHTOKEN` env** (not a `--authtoken` arg — a secret must never appear in `ps`), polls the `:4040` inspector (pure `extractNgrokUrl` prefers the first `https://` `public_url`), and **bails the instant the child exits** (`exitCode`/`signalCode` set — a bad authtoken makes `ngrok` exit at once) rather than spinning to the ~15s timeout; `null` on no-authtoken/absent-binary/no-URL/child-death, never throws. `probe` = spawn + URL + `fetch(url/health)`, killing the child in a `finally` (single teardown site). Verified `child.kill()` on an already-exited child returns `false` **without throwing** (no ESRCH) — so the null-path teardown needs no `try`/`catch` (the `!child.killed` guard is enough). Tests: the spawn fake is a **real `node -e` subprocess** (real `.kill()`/`exitCode` plumbing — mirrors `cloudflare.test.ts`'s `fakeCloudflared`); the public URL comes from an **injected fake `fetch`** answering the `:4040` JSON (+ `/health`), because the inspector is HTTP not stderr (a fake subprocess can't synthesize an HTTP response) — 21 tests (5 `extractNgrokUrl` pure + 3 shape/presence incl. a PATH-stripped default + 6 start + 7 probe). Removed `@ngrok/ngrok` from `dependencies`; `npm install` dropped 2 pkgs, `package-lock.json` has **0** `@ngrok/ngrok` refs, and npm's leftover empty `node_modules/@ngrok` scope dir was removed. The only remaining `@ngrok/ngrok` mention in `src/` is the provider's doc comment explaining the revision (intentional). Trio green (598 passed / 1 skipped); `test:compose` green (prod image builds + serves with the dep gone). The live `*.ngrok.app` round-trip stays gated (`T-ngrok-channel-e2e`, `ngrok` binary + `NGROK_AUTHTOKEN`).

### Tool orientation `guide` (spec 18)

A single `guide` tool returns a concise, evolvable orientation (what OpenHammer is, the 7 tools, and the **working-root contract**: paths resolve under `MCP_ROOT_DIR`; `bash` cwd resets each call; use absolute paths). This makes the working directory obvious to the client **without bloating every tool's description** — definitions stay precise; the guide is read once and evolves as the MCP grows. Spec: `specs/18`. **Sessions/memory (the earlier Phase B) are deferred** — moved out of the loop into `docs/agent-harness-design.md` because they conflict with that doc's stateless thesis (see below).

- [x] **18a.** `src/tools/guide.ts` (`guideTool: ToolModule`): name `guide`, no params, description *"Read this first — how OpenHammer's tools work + your working root"*; returns markdown built from `config.rootDir` (what OH is; **working root `<abs>` + "paths resolve under it, bash cwd doesn't persist, use absolute paths"**; the 7 tools one line each; bounded output/`response_too_large` backstop; no memory between calls; container = jail). Register in `createAllTools` (an 8th tool; the 7 capability descriptions stay **lean & unchanged**). Also verify `read`/`write`/`edit`/`grep`/`find`/`ls` emit **absolute resolved paths** in results. + tests. *deps: 10, 03–09.* **Deviation recorded:** replaces the earlier "bake the working root into every tool description" idea — rejected as repetitive bloat. One evolvable `guide` tool read once. (`InitializeResult.instructions` is the auto-delivered alternative; revisit if clients don't call `guide`.) **Shipped:** the pure markdown builder is split into an exported `buildGuide(rootDir): string` (the `formatStartup`/`extractTunnelUrl` "export the pure testable part" precedent) and `guideTool.execute(_args, rootDir)` wraps it in a single text `ToolOk` — no params (`inputSchema: { type:"object", properties:{} }`). Registered **first** in `MODULES` (`[guideTool, readTool, …, lsTool]`) so "read this first" is the `tools/list` order; `ToolModule.name`'s literal union gained `"guide"` (single source). The 7 capability tools are **byte-unchanged** (lean descriptions intact — verified by eye + 18b will pin it). The "7 tools" identity → "7 capability tools + `guide`": every `tools/list` count/name assertion updated across `index.test.ts`/`server.test.ts`/`mcp.e2e.test.ts`/`http-transport.test.ts` (7→8, name arrays gain `guide`) + the compose runner's `REAL_TOOL_NAMES` (now 8; the fixture-`echo` branch's `tools.length === 1` selection is unaffected — `test/compose/run-e2e.ts` is typechecked/linted but not in the trio), and stale `README.md` claims fixed ("The 7 tools" → "Tools" table w/ a `guide` row; "expect 7 tools" → "expect 8 tools"). 10 guide tests (7 `buildGuide` pure: root embedded, working-root contract `does not persist`/`absolute paths`, `no LLM`/`stateless`, all 7 capability names, workflow notes `response_too_large`/`no memory`/`container`, evolvability line, different-root verbatim; 3 `guideTool`: shape + empty schema, `execute`→`buildGuide` equality, args-ignored root-driven). Trio green (608 passed / 1 skipped).
	**Result-path honesty — verified, not rewritten (the spec's "most already do via `resolveToCwd`" reconciled):** the spec's intent ("a wrong-dir op is visible in the result, not silent") is met by `guide` itself surfacing the absolute working root (spec 18's thesis) + the fs tools' already-honest **error** paths. No tool output was changed, because the candidates are deliberately locked: `write`/`edit` echo the model's **original arg path** in success messages (`Successfully wrote N bytes to <arg>`) — this is **pi fidelity, explicitly locked** by `write.test.ts:104` ("reports the original path … not the resolved absolute path", `expect(text).not.toContain(rootDir)`) + the tools' doc comments; spec 18 does not override it. `grep`/`find` emit **relative-to-search-root** listing output by design (their own descriptions "Returns matching file paths relative to the search directory" + specs 07/08's `path:line: text` format) — rewriting them to absolute would contradict those specs. `ls`/`grep` **error** messages already carry the absolute resolved path (`Path not found: <abs dirPath/searchPath>`); `read`/`ls` success output echoes no path (nothing to absolutize). So "fs results show absolute resolved paths" holds where a path is echoed in an error, and the absolute working root is carried by `guide`; the locked pi-faithful + by-design success outputs stand. **18b** remains the cross-cutting test checkbox — its "fs results absolute" assertion targets an fs error surfacing the absolute resolved path (e.g. `ls`/`grep` missing-path → `Path not found: <abs>`), which is already true.
- [x] **18b.** tests: `guide` returns the root + contract; `tools/list` includes `guide`; the 7 capability descriptions are unchanged; fs results absolute. *deps: 18a.* **Deviation recorded:** the cross-cutting spec-18 Phase A acceptance suite lives in a **new** `src/tools/guide-acceptance.test.ts` (6 tests) — separate from `guide.test.ts` (the guide *module* units) because 18b spans *other* tools' concerns (their descriptions, their fs error paths) + a registry-level integration view. Each assertion drives the real tool modules **through `createAllTools`** (the registry surface `tools/list` reads), complementing (not duplicating) `guide.test.ts`/`index.test.ts`/`server.test.ts`: (1) `guide`'s handler returns the resolved root + the `does not persist`/`absolute paths` contract; (2) the registry is `guide` first then the 7 capabilities in stable order (the "7 + guide" identity); (3) **the genuinely new coverage** — the 7 capability descriptions are (a) **pinned** to the v1 baseline via exact `.toBe(EXPECTED_DESCRIPTIONS[name])` equality and (b) **lean** (none contains a working-root token — `working root`/`does not persist`/`resets to the root`/`mcp_root_dir`), encoding spec 18 line 16's "lean — no per-tool working-root line"; `index.test.ts` only checked `typeof description === "string"`, so a working-root-line bloat previously slipped through; (4) **fs results surface the absolute resolved path** — `ls` + `grep` of a missing *relative* path return `Path not found: <join(rootDir,rel)>` through the registry handler (grep's unit test pinned only the `Path not found:` prefix, never the absolute path). **Type honesty:** `McpToolEntry.tool` is the SDK `Tool` type, whose `description` is *optional* (`string | undefined`) — so the description is read via a `typeof desc !== "string"` narrowing helper (`descriptionOf`), **not** built into a `Record<string,string>` (that assignment was the first cut's typecheck failure: optional → `string`). No `as`/`!`; `noUncheckedIndexedAccess` stays OFF (so `entries[0]`/`CAPABILITY_NAMES[i]` index without undefined). `EXPECTED_DESCRIPTIONS` pins the v1 baseline with the constants **resolved** to their values (DEFAULT_MAX_LINES 2000 / DEFAULT_MAX_BYTES→`50*1024/1024`=**`50KB`** (not `50.0KB` — raw `${/1024}`, not `formatSize`); per-tool limits grep 100 / find 1000 / ls 500; GREP_MAX_LINE_LENGTH 500) — single-source values documented in-comment; if a truncation constant legitimately changes, the source description + this baseline move together (the "unchanged" guard re-pins consciously). **No snapshots** (the repo uses none) — explicit assertions, repo style. This is pure verification of already-shipped behavior (the 18a note: "fs results absolute … which is already true") — **no `src/` changes**; trio green (614 passed / 1 skipped).
- **Deferred (NOT a pickable checkbox) — sessions & memory:** per-session cwd + memory was sketched (MCP `sessionIdGenerator` + pi-harness `ExecutionEnv` + JSONL at `~/.openhammer/sessions/`). It is **removed from the loop** and recorded in `docs/agent-harness-design.md`, because it conflicts with that doc's thesis (stateless substrate; memory = files under `MCP_ROOT_DIR`; provider owns the loop; server sessions rejected in Part 5A). Reconcile — likely agent-as-directory pins a working root per agent, not server session state — before actioning. See `specs/18` "Deferred" + `specs/99`.

### TUI control center (spec 19)

One live, full-screen TUI — the **control center** — that replaces isolated subcommands for interactive use: server status + channels (live) + connected clients + a streaming monitor feed, all driven from a key menu, with add/config wizards as modals. It is a **view over the running server** (subscribes to the spec-17 status socket + reads settings), optionally managing the server lifecycle (start/stop a child) so `openhammer` is the single entry. Spec: `specs/19`. The render substrate is a `19a` decision: a live dashboard needs a **render loop** (clack can't do this — it stays for the wizards, which run as modals); options are pi-tui (re-adopted for this layer — its render loop is the point, not the chat half), hand-rolled ANSI (zero dep), or ink.

- [x] **19a.** `src/tui/dashboard/render.ts` — **pi-tui** `DashboardRenderer` adapter (raw-mode stdin, differential redraw, resize-safe). Add `@earendil-works/pi-tui` to **devDependencies**. *deps: 17.* **Deviation recorded (decided, evidence-based):** **pi-tui** chosen — purpose-built (pi's live interactive dashboard runs on it), 0.80.x / 2 deps / ~1.7MB / updated daily, handles the streaming monitor feed + resize that hand-rolling risks. Rejected: **ink** (25 deps + React — too heavy); **hand-rolled** (streaming-feed/resize redraw is the bug-prone part). Layer-specific, not a reversal of spec 17 (clack for wizards, pi-tui for the dashboard). **Shipped:** `@earendil-works/pi-tui@^0.80.2` → devDependencies (`npm install` added 2 pkgs — the `marked` chat-half dead weight the spec acknowledges); `src/tui/dashboard/render.ts` exports the `DashboardRenderer` seam + `FrameProducer` type + `createDashboardRenderer(deps: DashboardRendererDeps = {})` (the `11a`/`13`/`17b`–`17t` injection-arg precedent). The impl mounts a `FrameComponent` (the pi-tui `Component { render(width): string[] }` root) on a `new TUI(terminal, showHardwareCursor)`, calls `tui.start()` (raw-mode stdin + the differential render loop + resize detection + synchronized output — **all pi-tui features the adapter activates, not reimplemented**), and `tui.stop()` (restores cooked mode/cursor/bracketed-paste/Kitty — the no-raw-mode-leak bar; idempotent via a `stopped` flag). Keys are intercepted via `tui.addInputListener` + forwarded to the `onKey` handler; **every key consumes + requests a redraw** because a consumed input returns early from the TUI's `handleInput` before its own `requestRender`, and an unconsumed one with no focused component never reaches it either (the dashboard is a key-menu view with no focused component; modals suspend the loop + run clack, so the listener is idle during a wizard). A configurable refresh interval (`refreshIntervalMs`, default 150ms; `0` disables) pulls fresh frames for async state (the status-socket feed, 17s/19c) without a per-event poke. **Deviations from spec 19 line 34's sketched `{ start(loop: () => void); stop(); onKey(cb: (key) => void); clear() }`:** (1) `loop: () => void` → `start(produceFrame: (width, height) => string[])` — pi-tui's root `Component.render(width): string[]` *is* the frame source the TUI differentially redraws; making the loop *return* the frame (and receive the terminal size for full-screen panel layout) is the natural pull model that's also directly unit-testable (a `void` loop would need an out-of-band frame-push method not in the spec interface). (2) `cb: (key)` → `cb: (data: string)` — pi-tui delivers raw input sequences; `matchesKey(data, "ctrl+c")`/`parseKey(data)` operate on that raw string, so delivering it verbatim is the faithful, flexible contract 19d's key menu builds on. The width-only `Component.render` → `(width, height)` producer adapter is a closure folding in `terminal.rows`. **Tested with a `FakeTerminal implements Terminal`** (the full 15-member interface, honestly — no `as` cast; inert cursor/clear/title ops) injected via `deps.terminal`, so the hermetic trio drives the **real `TUI`** render loop with no TTY: lifecycle (start/stop/idempotent-stop/stop-without-start), frame production (producer called with width+height on start + drawn; `clear()` → `requestRender(true)` → `fullRender(clear=true)` writes `\x1b[2J`), keys (raw-sequence delivery incl. `\x03`; every key triggers a re-render), resize (re-renders at the new width), + the refresh cadence. **One subtlety the tests pin:** pi-tui coalesces renders to ≥16ms (`MIN_RENDER_INTERVAL_MS`), so the test `flush()` waits ~25ms past the throttle. **No `as` casts** (the `addInputListener` callback is contextually typed against pi-tui's internal `InputListener`; `FrameComponent implements Component`). pi-tui is a **devDep** so the prod image (`main.js`, which never imports this) stays lean — `npm ci --omit=dev` won't install it; 19e will lazy-`import()` the dashboard from the CLI so the headless path never loads it. **Smoke:** the default renderer (real `ProcessTerminal` + `TUI`) constructs + `start()`/`stop()` round-trips cleanly — `start` emits the bracketed-paste/Kitty-enable sequences, `stop` emits their disable + `showCursor` (terminal restore proven with the real classes). 10 tests; trio green (629 passed / 1 skipped).
- [x] **19b.** `src/tui/dashboard.ts` + `src/tui/dashboard/panels.ts` — the render loop + the four panels (status / channels / clients / monitor) as pure `(state) => RenderOutput` functions. *deps: 19a, 17.* **Deviation recorded:** the spec's `runDashboard({ socketPath, settings, serverControl })` (spec 19 line 30) is **decomposed into a testable core with injectable seams** — the status-socket client (19c), the key-menu modals (19d), and the server lifecycle (19e) are *not* wired here (wiring them would mean stubs against not-yet-existing modules). `runDashboard(deps: DashboardDeps)` takes the seams they will fill: `renderer` (the 19a `DashboardRenderer`) + `settings` (required), then `subscribe` (the live event feed → 19c wires the real socket), `status`/`channelState` (initial reachability snapshots → 19e/19c), `keys` (the footer menu), and `onQuit` (shutdown → 19e stops the child). **19e will assemble the spec's public `{ socketPath, settings, serverControl }` entrypoint around this core** — that is its checkbox, not 19b's. The loop itself is complete: it composes the panels, refreshes on key/resize + the renderer's refresh cadence, folds live events into the monitor + clients panels, and quits cleanly (`q`/`Q`/Ctrl+C `\x03` → idempotent `stop()` + best-effort `onQuit`). **Panels are plain-text (no ANSI) — a deliberate v1 simplification:** every rendered value is ASCII (URLs, tokens, client names, `formatEvent` lines), so `.length` *is* the visible width and `clip(line,width)` (truncate + `›`) is correct with no ANSI-aware width calc; color (spec 19 §1.4) is a future polish, and keeping the panels ANSI-free keeps them pure + free of the pi-tui devDep (importing `panels.ts` never pulls the render lib into the unit-test graph). **Reuse, not reimplementation** (spec 19 line 49 — the dashboard is a *view* over the running server): the monitor panel reuses `formatEvent` from `monitor-view.ts` (17t); the clients panel reuses `isoTimeOf`; the render loop reuses `MonitorState` (17t) — the clients panel is derived from the **same active-client reducer** `openhammer monitor` uses, fed by the single `subscribe` feed (recent dump, then live). `subscribe` delivers parsed `RequestEvent`s (19c parses the socket's NDJSON via `parseEventLine`); the dashboard never touches the wire format. **`composeDashboard(state, width, keys?)` is width-only** (height-aware panel sizing is deferred — the monitor ring is capped at `monitorLimit` regardless); sections are blank-line-separated, each line `clip`ped to width. **Live updates ride the renderer's refresh cadence** (19a's `refreshIntervalMs`, default 150ms — the frame producer reads live state each tick, so subscribed events reach the screen without a per-event poke; the `DashboardRenderer` interface exposes only `clear()` (a full-screen clear), so a per-event poke would flicker). The `r`/`R` key forces an immediate full redraw via `clear()`. **Footer advertises only wired keys** in 19b (`r refresh`/`q quit` — `DEFAULT_19B_KEYS`); 19d extends `keys` with the modal keys (`a`/`c`/`d`) so the footer never shows a dead key. **Shutdown order:** `renderer.stop()` first (terminal restored — the no-raw-mode-leak bar from 19a), then `onQuit` best-effort (a throw is caught + logged to stderr, never rejects the loop). **30 tests** (19 `panels.test.ts` pure: status up/down + URL/token omission, channels rows/marker/live-vs-declared-url/down, clients singular/plural/time, monitor tail-limit, footer join, compose layout/separators/clip/width-0/default-keys; 11 `dashboard.test.ts` via a `FakeRenderer` that captures the producer + key handler: initial 4-panel render, channels-from-settings, live event → monitor+clients, ring cap, default-limit, `q`/Ctrl+C quit, `r` clear, idempotent onQuit, unsubscribe-on-quit, async-onQuit awaited + throw-swallowed); trio green (659 passed / 1 skipped).
- [ ] **19c.** status-socket client: dashboard subscribes to the NDJSON stream → live clients + monitor feed; queries channel live-state. *deps: 19b, 17s.*
- [ ] **19d.** key menu + embedded clack wizards as modals (add channel / config / doctor). *deps: 19b, 17k, 17l.*
- [ ] **19e.** `src/tui/dashboard/server-control.ts` + `src/cli.ts` — server lifecycle (start/stop the server as a child); `openhammer` no-args (TTY) launches dashboard + server; clean shutdown, no orphan. *deps: 19b, 14b.*
- [ ] **19f.** tests (panel pure functions; renderer via injectable; lifecycle start/stop). *deps: 19b.*

### OAuth authorization server (spec 20)

A minimal OAuth 2.0 **client-credentials** AS so OAuth-only MCP clients (which can't set a raw `Authorization: Bearer` header) connect natively: `POST /oauth/token` issues an HS256 JWT against `client_id`+`client_secret`; RFC 8414/9728 metadata advertises it; the auth middleware accepts the existing opaque token **or** a valid JWT. **Client-credentials only** (no auth-code/login/users/DB/refresh). Spec: `specs/20` (has full code examples). **Un-defers spec 11's "no OAuth AS."**

- [ ] **20a.** `src/auth/oauth/jwt.ts` — HS256 sign/verify via **`jose`** (`SignJWT`/`jwtVerify`; alg-confusion-safe). Add `jose` to **dependencies** (prod server verifies JWTs). + tests (round-trip; tamper/expired/wrong-aud → null). *deps: none (jose is 0-dep).* **Deviation recorded (decided, evidence-based):** **jose** chosen — 0 runtime deps + audited, and `jwtVerify` blocks alg-confusion (`alg:"none"`/`RS256`) that hand-rolling risks; the no-`jose` rule was for the v1 opaque token, not an OAuth AS.
- [ ] **20b.** `src/auth/oauth/clients.ts` — client registry over `~/.openhammer/credentials.json` (`newClientId`/`newClientSecret`/SHA-256+`timingSafeEqual` `verifySecret`/issue/list/remove) + mint a `jwtSecret`. + tests. *deps: 17e.* **No `bcrypt`** (high-entropy secrets → SHA-256 + constant-time is adequate).
- [ ] **20c.** `src/auth/oauth/token.ts` + mount `POST /oauth/token` (client-credentials grant; `@fastify/formbody` for form posts) + RFC 8414 `/.well-known/oauth-authorization-server` + extend RFC 9728 to advertise it. + tests (valid/invalid pair; unsupported grant). *deps: 20a, 20b, 11c.*
- [ ] **20d.** extend `src/auth/middleware.ts` (spec 11b): accept opaque **or** JWT; `allowedClients` applies to the JWT `client_id`. + tests (opaque pass; JWT pass; expired/tampered → 401; disallowed client → 403). *deps: 20a, 11b, 17r.*
- [ ] **20e.** `openhammer auth {add-client|list|remove}` — TUI/CLI issuance; plaintext secret shown once. + tests. *deps: 20b, 17n.* *(This is the "set my own client credential via the TUI" flow.)*
- [ ] **20f.** `doctor` checks `credentials.json` perms + `jwtSecret` present; document `OAUTH_JWT_SECRET` in `.env.example`/README. *deps: 20b, 17p.*

- [ ] **20g.** *(headless / server deploy)* non-interactive flag mode for the config commands so they run **without a TTY**: `channel add --provider ngrok --authtoken "$T" [--default]`, `config set <section>.<key> <value>` (e.g. `mcp.allowedClients claude-code`), `auth add-client --label <name> [--print-secret]`. Each **reuses the existing `addChannel`/`setSection`/client-issue functions** via a flag-derived `io` (no clack) — identical behavior to the wizard, just scriptable/CI-friendly. + tests (flag path ≡ wizard path on the same inputs). *deps: 17k, 17l, 20e.* **Why:** a server deploy has no terminal; env + dotfile-provisioning cover most of it, but validated ergonomic channel/credential config needs a non-interactive CLI.

### UI consolidation — pi-tui only (spec 21)

Drop `@clack/prompts` + `ora`; consolidate ALL interactive UI (dashboard + wizards) on `@earendil-works/pi-tui` — matches pi (which uses only pi-tui, zero prompt libs). The wizards are **io-agnostic** (injectable `PromptIo` + probe runner, fully faked in tests), so only the two production impls change — `prompts.ts` (clack → pi-tui) + the channel-wizard probe spinner (`ora` → pi-tui `Loader`). Spec: `specs/21` (thorough, with the pi-tui prompt-adapter design). **Supersedes** the clack/ora mentions in 17a/17b + spec 19's "clack modals" → pi-tui overlays.

- [ ] **21a.** `src/tui/prompt-loop.ts` — `runPrompt` (boot a pi-tui loop, mount the component root, await its callback, teardown + **terminal restore on completion/Ctrl-C/error** — no raw-mode leak). + tests (virtual terminal / fake `ProcessTerminal`; restore-on-Ctrl-C). *deps: 19a.* **Deviation recorded:** pi-tui is a render-loop component system, not a one-shot prompt lib — this adapter is the seam that makes its components usable as async prompt functions (how pi does it). Terminal restore on every exit path is the correctness bar.
- [ ] **21b.** rewrite `src/tui/prompts.ts` (17b) on pi-tui over `runPrompt`: `askSelect`=SelectList, `askText`/`askSecret`=Input (masked), `askConfirm`=SelectList, `withSession`=banner header. + tests (PromptIo contract; the existing wizard tests pass unchanged — the injectable-io design is what keeps this bounded). *deps: 21a.*
- [ ] **21c.** channel-wizard default probe runner: `ora` → pi-tui `Loader`. + tests (injectable runner unchanged). *deps: 21a.*
- [ ] **21d.** remove `@clack/prompts` + `ora` from `devDependencies`; `npm install` (regen lockfile); `grep -rn "@clack/prompts|from \"ora\"" src test README docs` → remove stragglers. *deps: 21b, 21c.*
- [ ] **21e.** spec/docs sweep: spec 17 (17a deps, 17b prompts), spec 19 (modals → pi-tui overlays), README, the plan's 17a note → "clack/ora dropped, see spec 21." *deps: 21d.*

---

## Testing & pipeline (specs 15 + 16)

**Deterministic, no LLM.** The "real MCP client" is the official `@modelcontextprotocol/sdk` `Client`
driven by a script that asserts on `callTool` text. Five tiers build on each other — Tier N only exists
once Tier N−1's underlying code has shipped:

| Tier | Proves | Runs via | Activates |
|---|---|---|---|
| 0 Unit | each tool's `execute → Result` directly | `npm test` | every tool task 03–09 |
| 1 In-process MCP | SDK client ↔ `buildFastify` on port 0 (init/list/call, auth 401, backstop) | `npm test` | `12b` |
| 2 Boot | spawn entrypoint; `/health`, token reuse, clean shutdown | `npm test` | `14b` |
| 3 Containerized | real image + runner on a Docker network | `npm run test:compose` | fixture now; real server `14b` |
| 4 Tunnel | runner through a public URL (`trycloudflare.com` or `*.ngrok.app`) | `npm run test:tunnel` / `test:tunnel:ngrok` (gated) | `13`, `17` |

**Fast path (every iteration):** hermetic trio — `npm test` (Tiers 0–2), `npm run typecheck` (`tsconfig.json` + `tsconfig.test.json`), `npm run lint` (`biome check src test`). Green on any Node 20+ box; `loop.sh` unchanged.
**Container path (on-demand, non-blocking):** `npm run test:compose`, `npm run test:in-container` (`docker compose run --rm dev npm test`), `npm run test:tunnel` (gated on `OPENHAMMER_TUNNEL_E2E=1` + `cloudflared`), `npm run test:tunnel:ngrok` (gated on `NGROK_AUTHTOKEN`, spec 17).

### Test-pipeline foundation (no src deps — can land alongside the scaffold)

- [x] **T-harness.** `vitest.config.ts` (include `src/**` + `test/e2e-hermetic/**`); `tsconfig.test.json` (typechecks `src`+`test`, no `rootDir`); `package.json` (`typecheck` + `lint`/`format` cover `test`; `test:compose`/`test:in-container`/`test:tunnel` scripts); `scripts/test-tunnel.sh`; `.gitignore`/`.env.example`. *deps: 01-scaffold-a.* **Deviation recorded:** `tsconfig.test.json` `extends` `tsconfig.json` with `noEmit:true` + `rootDir:"."` + `include:["src/**/*","test/**/*"]` — `rootDir:"."` (not unset, which JSON can't do) widens the inherited `src`-only rootDir so `test/**` is in scope; `outDir` is inert under `noEmit`; `rewriteRelativeImportExtensions` stays valid (allowed with `noEmit`). The dual `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit` typechecks `src` twice (belt-and-suspenders per spec 15) — harmless; the test half currently matches no `test/**` files (none exist yet → it typechecks `src` only) and turns on for real at `T-canary`. **Lint noise note:** `biome check src test` emits a non-fatal `internalError/io: No such file or directory` for the not-yet-existent `test/` dir and **exits 0** — verified, so the trio stays green; the message self-clears the moment `T-canary` creates `test/e2e-hermetic/harness.canary.test.ts` (don't read it as a real failure). `test:compose`/`test:in-container` are **defined now but non-functional** until `T-dockerfile`/`T-compose` ship `Dockerfile`+`docker-compose.yml` (expected — they're on-demand/non-blocking, never part of the trio). `test:tunnel` ships **gate+skip only** per spec 16 (`OPENHAMMER_TUNNEL_E2E=1` **and** `cloudflared` on PATH, else exit-0 skip); full compose `tunnel`-profile orchestration is deferred to `T-tunnel-e2e`. **`.gitignore`/`.env.example` reviewed, no change needed** — both are already pipeline-complete (`node_modules`/`dist`/`coverage`/`.env`/`.loop-complete`/`.claude` ignored; all 6 app env vars documented); `OPENHAMMER_TUNNEL_E2E` is a CI test gate documented in the script + specs 15/16, intentionally **not** added to `.env.example` (which is copied into runtime env — seeding a copy with the gate on would be a footgun).
- [x] **T-canary.** `test/e2e-hermetic/harness.canary.test.ts` + `test/fixtures/minimal-mcp-server.ts` — Tier-1 walking skeleton: SDK client ↔ in-process fixture on port 0 (connect/listTools/callTool, missing+wrong bearer 401, backstop). *deps: T-harness.* **Deviation recorded:** the fixture is **deliberately standalone — no `src/` import** (spec 16 mandates "mirrors"/"standalone"/"reference"). It replicates the bearer preHandler (`timingSafeEqual` + length-guard + per-request `Host`-derived `WWW-Authenticate` challenge), the per-request stateless `POST /mcp` transport wiring (header flush + `reply.hijack()`, copy-adapted verbatim from `src/mcp/http-transport.ts`), and the universal size backstop — inline, compactly — with **one** deterministic `echo` tool. A canary's job is to prove the *harness* works (vitest `test/e2e-hermetic/**` include, real SDK `Client` over loopback HTTP, ephemeral port-0 listen) against a known-good target, NOT to re-test the production server (that's `T-mcp-e2e` with the real `buildFastify`); keeping it src-independent means a mid-refactor `src/` can't turn the canary red. The real auth+backstop stay independently covered by `src/mcp/{server,http-transport}.test.ts` + `T-mcp-e2e`. Exports both `buildFixtureServer({token,maxResponseBytes?,logLevel?})` (build-only, **no listen** — the canary owns port-0 binding) and a standalone `main()` (the compose `fixture-server` CMD — read env, listen, `SIGINT`/`SIGTERM` shutdown). The auto-run guard `process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href` is **inert under vitest** (verified: `npm test` showed only ephemeral-port `Server listening` lines, no port-3000 binding, no hang — main never fired) and runs only under a direct `tsx test/fixtures/minimal-mcp-server.ts`. The canary drives a real SDK `Client` for connect/`listTools`/`callTool`/backstop, and raw Node-22 `fetch` for the two 401 cases (no point spinning a client just to test the gate); the `echo` backstop sums the single text block directly (echo is text-only — mirrors `src/mcp/server.ts`'s text+image sum). Building the fixture here (one file, full spec-16 surface) is right because T-canary *names* that file — T-compose later wires compose around it, it doesn't re-write it. **Bonus:** the T-harness-predicted `biome check src test` `internalError/io: No such file or directory` noise for the not-yet-existent `test/` dir is now **cleared** — `biome check` reports "Checked 53 files" cleanly.
- [x] **T-dockerfile.** `Dockerfile` (multi-stage `dev`/`build`/`prod`) + `.dockerignore`. *deps: none.* **Deviation recorded:** the 3 stages share a `base` stage (`node:22-bookworm-slim` + `apt-get install ripgrep fd-find ca-certificates` + `ln -s "$(command -v fdfind)" /usr/local/bin/fd`) — `base` carries the runtime binaries both `dev` (so in-container Tier-0 grep/find tests resolve) AND `prod` (so the real server's grep/find tools resolve) presence-check; spec 16's literal stage list omits them but spec 08's env note makes `fd-find`+symlink mandatory (and `ripgrep` follows for the `grep` tool). `dev` does `npm ci` (all deps) + `COPY . .` + `CMD ["npm","test"]`; `build` is `FROM dev` (reuses the deps+source layers) + `RUN npm run build` → `dist/` (no rebuild of the apt/deps layers); `prod` does `npm ci --omit=dev` + `COPY --from=build /app/dist` + `ENV MCP_ROOT_DIR=/data` + `CMD ["node","dist/main.js"]`. `prod` runs as the non-root `node` user (uid 1000) bundled in the base image — `mkdir /data && chown node:node /data` makes the tool root writable; `/home/node` is already node-writable so `ensureToken` can mint `~/.openhammer/credential.json` (or set `MCP_AUTH_TOKEN` to skip the file). `.dockerignore` excludes `node_modules`/`dist`/`coverage` (regenerated in-image, and a host copy would override `npm ci` and break platform resolution), `.git`, `.env`/`.env.*` (keep `.env.example`), `.loop-complete`, `.claude`, logs, and the docs/planning markdown (no stage needs them). **Validated by building the `prod` target** (all 21 steps green; the "5 high severity" npm-audit note is the known deferred `fastify@4`/`fast-uri` advisory) + a runtime smoke: image runs as uid 1000, `rg`→`/usr/bin/rg` (13.0.0), `fd`→`/usr/local/bin/fd` (fdfind 8.6.0 symlink), `dist/main.js` present; boots clean — `GET /health`→200, `POST /mcp` no-bearer→401, with-bearer→non-401 (auth gate proven).
- [x] **T-compose.** `docker-compose.yml` (`dev` profile-gated, `fixture-server` w/ healthcheck, `test-runner` depends_on healthy, `server` profile=real, `cloudflared` profile=tunnel) + `test/compose/run-e2e.ts`; `npm run test:compose` green (fixture). *deps: T-dockerfile, T-canary.* **Discovery (from T-dockerfile smoke):** every network-reachable server service (`fixture-server`, real `server`) MUST set `HOST=0.0.0.0` — `loadConfig` defaults `HOST=127.0.0.1` (spec 01), which binds loopback *inside* the container and is unreachable over the Docker network / via `-p` port-mapping (confirmed: smoke `curl` returned HTTP 000 until `HOST=0.0.0.0` was set, then `/health`→200). Wire this into the `environment:` of both server services. **Deviation recorded:** all 5 services ship per spec 16's topology — `fixture-server` + `test-runner` are **unprofiled** (plain `docker compose up` starts only them, verified), `dev`/`server`/`cloudflared` are profile-gated so a finishing service can't abort the E2E. **One shared image, two tags:** `fixture-server`/`test-runner`/`dev` all build the `dev` stage and tag `openhammer:dev` (compose builds once, reuses across all three via shared `image:`+`build:`); `server` builds `prod` → `openhammer:prod`. The healthcheck is node 22's global `fetch` probing `/health` in **exec form** (`CMD node -e "…"` — the `-e` script is one arg, no shell quoting, no `curl` dep, no sleep; `start_period:3s interval:2s retries:30`) and `test-runner`'s `depends_on: fixture-server condition: service_healthy` waits on it. `tsx` runs via `./node_modules/.bin/tsx` (spec decision — no npm wrapper). `test/compose/run-e2e.ts` is **standalone — no `src/` import** (the fixture is the target; the real server's 7-tool coverage is `T-real-compose`'s job): it mirrors the canary client exactly — `new Client` over `StreamableHTTPClientTransport(new URL(MCP_URL), {requestInit:{headers:{authorization:\`Bearer ${MCP_TOKEN}\`}}})` → connect → listTools → callTool, narrowing `callTool`'s union via `firstText` with **zero `as`** (guards carry it), asserting with `node:assert` (default import; `esModuleInterop` makes it typecheck), then `process.exit(0|1)`. It lives under `test/compose/` — **outside** the vitest `test/e2e-hermetic/**` trio include (so `npm test` never loads it) but **inside** `tsconfig.test.json`/biome's `test/` scope (typechecked + linted). `dev` is **image-baked, no volume mount** (the spec's "reproducible" wording argues against a host bind mount; `test:in-container` runs the `COPY . .` source — rebuild with `docker compose build dev` to pick up edits). `cloudflared` `depends_on: server` (profile `real`), so the tunnel run is `--profile real --profile tunnel` together (validated: cloudflared renders under both, omitting one drops it as unsatisfiable — that's `T-tunnel-e2e`'s invocation, not a defect). **Validated end-to-end:** `npm run test:compose` built the image, fixture-server went `Healthy` on `0.0.0.0:3000`, test-runner connected/listed(`echo`)/called(`hello compose` echoed) over the Docker network → `ALL CHECKS PASSED` → exit 0 → clean `--abort-on-container-exit` teardown (fixture SIGTERM'd, exit 0).

### Higher tiers (dependency-ordered; each is its own iteration)

- [x] **T-mcp-e2e.** `test/e2e-hermetic/mcp.e2e.test.ts` — Tier-1 real: swap the fixture for real `buildFastify`; drive all 7 tools over `POST /mcp` (port 0) + assert auth 401 + `response_too_large` backstop. *deps: 12b.* **Deviation recorded:** the suite mirrors the canary's `withServer`/`connectClient`/`firstText` shape exactly but drives the **real `buildFastify`** (CORS + `/health` + well-known + bearer-gated `/mcp`) on an ephemeral port-0 listen — the fixture is gone. Each tool is the **sole subject under test**: fixture setup + verification go through `node:fs` (not another tool), so a broken `write` can't turn the `read` test red. A `callToolOnce(baseUrl,name,args)` helper (connect → callTool → close) drives each of `read`/`bash`/`write`/`edit`/`grep`/`find`/`ls` in its own `it` over `POST /mcp`; `listTools` uses a held client to assert all 7 names. `write`/`edit` also assert on-disk side effects (`readFileSync`) and the exact `Successfully wrote` / `Successfully replaced 1 block(s)` contracts; `read`/`grep`/`find`/`ls` assert on `callTool` text. `withServer` builds a fresh `Config` + temp `rootDir` (`mkdtempSync`, `rmSync` in `finally`) per test — isolation only, since the app is already stateless (per-request `Server` + `Transport`). `logLevel: "silent"` is load-bearing: `buildFastify` always constructs a pino logger, and without `silent` the 11 tests flood `npm test` with per-request `level:30` info lines. Auth 401 is asserted via raw `fetch` (no-bearer + wrong-bearer), matching the canary — no point spinning an SDK client just to test the gate. The backstop drives the **real** `src/mcp/server.ts` cap: `maxResponseBytes: 8` + `bash` `echo` of 200 chars fires `response_too_large` as a structured success-payload (`isError:false`, `{ok:false,error:"response_too_large",cap:8}`); 200 chars is under bash's own 50KB truncation, so it is the whole-response ceiling — not per-tool truncation — that fires. **Zero `as` casts** (guards carry the `callTool`-union narrowing, same as the canary + `http-transport.test.ts`); this is the file that 12b's `http-transport.test.ts` comment forward-references.
- [x] **T-boot-e2e.** `test/e2e-hermetic/boot.e2e.test.ts` — Tier-2: spawn entrypoint via `tsx`, assert `/health`, token reuse across two boots, clean `SIGINT`/`SIGTERM` (no orphan tunnel child). *deps: 14b.* **Deviation recorded:** the child is the real `node_modules/.bin/tsx src/main.ts` (no `dist` build dependency — `tsx` compiles TS on the fly, mirroring `npm run dev`). A free loopback port is allocated up front (`net.createServer` listen 0 → read → close) and handed to the child as `PORT`, so the banner port === the bound port === the port the test probes (passing `PORT=0` would make `config.port` 0 and the banner print `:0` — no way to read the actual port back). The accepted TOCTOU race (port grabbed between close + child listen) is fail-fast: the child dies on `EADDRINUSE` and `waitForReady` rejects with the captured stdout/stderr instead of polling the full timeout. Hermetic by env curation: an isolated `HOME` (which `os.homedir()` honors on POSIX, verified) forces `ensureToken` to mint into a temp `~/.openhammer`, `MCP_ROOT_DIR` is a temp dir, `LOG_LEVEL=warn` keeps output quiet, and the parent `MCP_AUTH_TOKEN` is **deleted** from the child env (no override leaks from the test runner) so the credential-file path — not the override short-circuit — is what the reuse test exercises. `waitForReady` polls `GET /health` for readiness and races a one-shot `child.exit` listener (no dangling rejecting-promise: the exit flag is read in the poll loop, so a post-readiness exit can't create an unhandled rejection). `waitForToken` re-polls stdout for the banner token because `listen` resolves in `main.ts` a hair before `printStartup` runs — health can be 200 before the token line flushes. **5 `it`s**: `/health` 200; token reuse across two boots (asserts identical banner tokens **and** that `~/.openhammer/credential.json` holds the same token); clean `SIGINT` → exit 0; clean `SIGTERM` → exit 0; and the tunnel slice `skipIf(isToolAvailable("cloudflared"))` — booting `--tunnel` with cloudflared **absent** logs the `continuing localhost-only` warn (`startTunnel` returns `null` before spawning, so there is no child to leak) then exits 0. The live-`cloudflared` "no orphan tunnel child" assertion is non-deterministic in the hermetic trio (a real child holds a live Cloudflare edge connection), so it is `skipIf`-gated to `T-tunnel-e2e`; this file's tunnel `it` proves the `--tunnel` argv wiring + null-handling + clean shutdown deterministically. `ensureDead` (SIGTERM → SIGKILL backstop) + `rmSync` in every `finally` guarantee no leaked children or temp dirs on failure. **Zero `as` casts** (`server.address() as AddressInfo` is the documented Node narrowing exception, matching `mcp.e2e.test.ts`); validated end-to-end: 5/5 green in 2.24s (tsx cold-start ~1s/boot), 262/262 across the full trio.
- [x] **T-real-compose.** `server` (profile `real`) + retarget `test/compose/run-e2e.ts` at `http://server:3000/mcp` with all 7 tools; add `test:compose:real` script. *deps: 14b, T-compose.* **Deviation recorded:** `run-e2e.ts` now **self-selects** its assertion suite from `tools/list` — `1 tool named "echo"` → the fixture `echo` round-trip (unchanged, `test:compose`), else the **real 7-tool sweep** (`test:compose:real`) — so "retarget" is purely `MCP_URL` and a single runner script covers both compose modes (spec 16: "the assertions grow to cover all 7 tools"). There is **no shared filesystem** between the runner container and the `server` container, so the file-based tools are seeded via the server's own `write` tool (rooted at `MCP_ROOT_DIR=/data`, auto-creating parent dirs) before `read`/`grep`/`find`/`ls`/`edit` assert on them — held over one connected SDK `Client` (stateless server ⇒ multiple requests are independent). A `real-test-runner` service (profile `real`) + `server` `/health` healthcheck were added: the healthcheck lets the runner `depends_on: service_healthy` (mirror of `fixture-server`) instead of racing a not-yet-listening server; `test:compose:real` = `docker compose --profile real up --build --exit-code-from real-test-runner --abort-on-container-exit real-test-runner` — **naming only `real-test-runner`** is load-bearing: compose starts it + its `server` dependency and leaves the unprofiled `fixture-server`/`test-runner` untouched (verified — only `server` + `real-test-runner` attached), so a finishing fixture runner can never abort the real run. **Bug found + fixed (the integration regression this tier exists to catch):** the real `find` tool failed in the container — Debian bookworm's `fd-find` **8.6.0** rejects `--no-require-git` (a flag the host's fd 10.4.2 accepts; it was added in a later 8.x). `src/tools/find.ts` now **feature-detects** the flag (`spawnSync("fd",["--no-require-git","--help"])` once per process, cached) and includes it only when supported — the host Tier-0 `.gitignore`-in-a-non-git-dir test still passes (fd 10.x ⇒ flag included ⇒ honored), and the prod container works (fd 8.6.0 ⇒ flag omitted ⇒ finds files; `/data` has no `.gitignore`, so the non-git `.gitignore`-honoring semantic the flag adds is irrelevant there). **Latent note (non-blocking):** `test:in-container` (dev image, fd 8.6.0) would still see the find `.gitignore` test fail (8.6.0 can't honor `.gitignore` in a non-git dir at all); it's non-blocking and outside the trio — revisit if `test:in-container` is promoted to a gate. **Validated end-to-end:** `test:compose:real` → `target: real server (7 tools)` → `tools/list ok: read, bash, edit, write, grep, find, ls` → `tools/call ok: all 7 tools round-tripped` → `ALL CHECKS PASSED` → exit 0; `test:compose` (fixture echo branch) still green (unbroken by the shared-runner rewrite).
- [x] **T-tunnel-e2e.** Wire `scripts/test-tunnel.sh` → compose `tunnel` profile: real `server` + `cloudflared`, parse `trycloudflare.com` URL from stderr, runner drives a `tools/call` through the public URL. Gated/non-blocking. *deps: 13, T-real-compose.* **Deviation recorded:** the orchestration lives entirely in `scripts/test-tunnel.sh` (T-harness shipped it as gate+skip-only); `docker-compose.yml` + `test/compose/run-e2e.ts` are **unchanged** — T-compose already provisioned the `cloudflared` service (`profile: tunnel`, `command: tunnel --url http://server:3000 --no-autoupdate`, `depends_on: server`) and the real `server` `/health` healthcheck, and the runner already self-selects its 7-tool sweep from `MCP_URL`/`MCP_TOKEN`. Gate (unchanged posture, non-blocking): skip→exit 0 unless **`OPENHAMMER_TUNNEL_E2E=1`** AND `cloudflared` on PATH AND `docker compose` v2 (added the docker gate — the trio must not flake on a compose-less box). Flow: `docker compose --profile real --profile tunnel up -d --build server cloudflared` → poll (≤90s) for **both** server healthy (`docker inspect -f '{{.State.Health.Status}}'` on the `server` container id from `compose ps -q`) AND a `https://[a-z0-9-]+\.trycloudflare\.com` URL in `docker compose logs cloudflared` (the **same regex as `extractTunnelUrl`** in `src/tunnel/cloudflare.ts` — single source of truth) → retarget the **existing** `run-e2e.ts` runner on the **host** via `./node_modules/.bin/tsx` with `MCP_URL=<url>/mcp` + `MCP_TOKEN` matching `server`'s `MCP_AUTH_TOKEN` → full 7-tool `tools/call` sweep through the **public** URL. `trap teardown EXIT` → `docker compose down --remove-orphans` win or lose (no leaked containers). **Runner-on-host (not a compose runner service):** reuses `run-e2e.ts` verbatim — single source of truth — and avoids a second image build (the `up` only builds `prod` + pulls `cloudflared`, while the host already has tsx + the SDK, which Tiers 1/2 depend on anyway); the SDK `Client` over `StreamableHTTPClientTransport` reaches `trycloudflare.com` via the host's internet egress, the bearer riding every request via `requestInit.headers` (no browser → CORS irrelevant; cloudflared forwards the `Authorization` header to `server`). `set -euo pipefail` is kept safe: the readiness helpers always exit 0 (so `$(...)` can't trip `-e`), the URL-extract pipeline ends in `|| true` (grep no-match + `head` SIGPIPE under `pipefail` are absorbed), and the runner runs under `if …; then exit 0; fi` so its non-zero code is captured by `rc=$?` rather than aborting. **Validated hermetically** (cloudflared is absent on the dev box — the documented `13` posture): `bash -n` clean, `docker compose --profile real --profile tunnel config` valid, and **both skip paths exit 0** (no-flag → "skip: OPENHAMMER_TUNNEL_E2E is not set"; flag-but-no-cloudflared → "skip: cloudflared is not on PATH"). The live-edge round-trip is inherently non-hermetic (it traverses Cloudflare's edge), so the sweep is **wired but not run in-tree** — the same posture as `13`'s `startTunnel` unit tests (injected fake subprocesses; the host binary path returns `null` naturally). A real run needs `OPENHAMMER_TUNNEL_E2E=1` + `cloudflared` + internet egress from Docker to the Cloudflare edge.

- [x] **T-cli-e2e.** `test/e2e-hermetic/cli.e2e.test.ts` — Tier-2: spawn `node dist/cli.js doctor` / `tunnel list` (isolated `HOME` so `~/.openhammer` is temp) / `start`; assert the **README banner is in the output**, exit codes, and a clean `SIGTERM`. *deps: 17m, 17n.* **Deviation recorded:** mirrors `boot.e2e.test.ts`'s isolated-`HOME` + ephemeral-port pattern; the banner assertion is the end-to-end proof that the TUI prints the README banner (the unit test in 17a pins the bytes; this proves they reach stdout through the CLI). **Shipped:** drives the real CLI as a genuine subprocess via **`tsx src/cli.ts`, NOT `node dist/cli.js`** — the bin is `dist/cli.js`, but `npm test` never builds `dist` (a stale/absent `dist` would make the test assert the wrong code), so tsx exercises the identical code path with **no dist build dependency**, mirroring `boot.e2e`'s exact rationale; the auto-run guard fires under `tsx src/cli.ts` exactly as it would under `node dist/cli.js`. The checkbox's `tunnel list` is a **stale name** — the real command (17o) is `channel list`; the test uses `channel list`. **The banner is TTY-gated** (`runCli` prints the README banner only when `process.stdout.isTTY === true`, spec 17n), and a piped child is *not* a TTY — so the banner-present assertions run the child under a **real pseudo-terminal via util-linux `script`** (`script -qec "stty cols 200 rows 50; tsx …" /dev/null`), the no-`node-pty`-dependency way to give a Node child a TTY (`node:child_process` cannot allocate a PTY; `node-pty` would be a forbidden native dep). `stty cols 200 rows 50` widens the PTY so the banner's 89-column lines don't wrap; the PTY's ONLCR (`\n`→`\r\n`) is normalized (`replace(/\r\n/g,"\n")`) before asserting, and the **full `BANNER` constant round-trips intact** (verified empirically — `toContain(BANNER)` is byte-equality, not a substring). `script -e` forwards the child exit code; the `-c` shell string is fixed (repo-rooted paths + literal subcommands — the documented shell-string exception like `bash`/the tunnel tools; no user-controlled operand). **5 tests:** `doctor` under PTY → banner + `Ran 4 check(s)` + `[pass]`, exit 0; `channel list` under PTY → banner + `No channels configured`, exit 0; `channel list` **piped** → banner **absent** (proves the `isTTY` gate from the other side) + the empty message, exit 0; unknown command → `Unknown command:` to stderr, exit 2; `start` → `GET /health` 200 then clean `SIGTERM` exit 0 (proves the CLI `start`/default case delegates to the spec-14 boot path — `boot.e2e` covers `main.ts` directly; this covers the dispatch seam). The two PTY tests are `it.skipIf(!HAS_SCRIPT)` (`isToolAvailable("script")`; util-linux `script` absent → non-blocking skip — the banner-on-TTY logic itself is unit-tested in `cli.test.ts` + `banner.test.ts`). Hermetic: isolated `HOME` (temp `~/.openhammer`), temp `MCP_ROOT_DIR`, parent `MCP_AUTH_TOKEN` stripped; `PATH` inherited so `tsx`/`rg`/`fd` resolve. The `start` boot delegation reuses `boot.e2e`'s `getFreePort`/`waitForReady`/`awaitExit`/`ensureDead` helpers verbatim. Zero `as` casts except the documented Node `server.address() as AddressInfo` narrowing (parity with `boot.e2e`). **Smoke-verified:** `dist/cli.js doctor` under `script` prints the banner + 4-pass diagnostics / exit 0; `dist/cli.js start` boots `/health` 200 + clean `SIGTERM` exit 0; `dist/cli.js frobnicate` → exit 2. Trio green (619 passed / 1 skipped).
- [x] **T-ngrok-channel-e2e.** `scripts/test-tunnel-ngrok.sh` + a `test:tunnel:ngrok` script — Tier-4 (gated): with `NGROK_AUTHTOKEN` set, start the real `server`, start ngrok via the channel provider, read the public `*.ngrok.app` URL from the inspector (no stderr-scraping), retarget `test/compose/run-e2e.ts` at `<url>/mcp`, drive the tool sweep through the public URL. Gated/non-blocking like `test:tunnel` (cloudflare). *deps: 17i, 17q, T-real-compose.* **Deviation recorded (17u revised the provider CLI-first — this task's plan text still read "from the SDK result"; the SDK is gone, so the shipped script drives the `ngrok` CLI exactly as `ngrokProvider.start` does):** the gate is **`NGROK_AUTHTOKEN` exported AND `ngrok` on PATH** (no docker gate — unlike the cloudflare Tier-4, this run is **host-only**); skip→exit 0 unless both hold. **Host-only orchestration** (the load-bearing design call): ngrok is a host CLI bridging a LOCAL port, so the real server runs on the host too — the same boot path `boot.e2e.test.ts` drives — and ngrok reaches it at `127.0.0.1:<port>`. The compose `server` is only reachable on the Docker network, which a host ngrok cannot address without port-publishing, so it is simpler to run the server on the host than to plumb a published port through compose (the server image/code is already container-validated by `test:compose:real`; this tier's job is proving the *tunnel* routes the round-trip). Reuses the existing `run-e2e.ts` runner (retargeted via `MCP_URL`/`MCP_TOKEN`) + `test-tunnel.sh`'s gate/teardown/`probe_url` shape verbatim. **The URL is read from the ngrok inspector API** (`GET http://127.0.0.1:4040/api/tunnels`) via a `node -e` snippet that mirrors `extractNgrokUrl` (prefer the first `https://` `public_url`) — the same source `ngrokProvider.start` polls, so there is **no** stderr-scraping and no cloudflare-style DNS-provisioning wait; the inspector returns the URL once provisioned (`{tunnels:[]}` meanwhile), and a short `<url>/health` probe guards the round-trip. **Spawn hygiene matches the provider exactly:** `ngrok http <port>` with `NGROK_AUTHTOKEN` inherited via env (never a CLI arg — a secret must never appear in `ps`); a bad authtoken makes ngrok exit at once, so the URL-wait loop `kill -0`s the pid and fails fast with the ngrok log rather than burning the budget. **Orphan-server bug found + fixed (the host-process lifecycle this careful smoke catches):** spawning the server via the **`tsx` CLI** orphaned it — `tsx`'s `cli.mjs` spawns a CHILD node that actually runs the server, and killing `$!` (the CLI) leaves that child reparented to init, still holding its Fastify port (confirmed: the listening child survived the first run's teardown). Fix: run the server via **`node --import tsx src/main.ts`** instead — the server runs in the MAIN process (an esbuild helper child self-terminates on parent death), so `kill_pid` (SIGTERM → 1s grace → SIGKILL) on `$!` reaps it completely (re-verified: no orphan, no held port, no temp leftovers). `kill_pid` is applied to both ngrok + the server in the `trap … EXIT` teardown (ngrok is a single Go process, so the SIGKILL backstop is belt-and-suspenders there). Isolated `HOME` (temp `~/.openhammer`, so the status socket never touches the real one), temp `MCP_ROOT_DIR`, `MCP_AUTH_TOKEN` set (→ `ensureToken` short-circuits, no cred-file write), `LOG_LEVEL=warn`. **Live-validated on the dev box** (which happens to have `ngrok` installed + configured): the script provisioned a real `https://<region>.ngrok-free.app`, the `/health` probe routed, and the runner drove the full 8-tool sweep (`guide` + the 7 capabilities) through the public URL → `ALL CHECKS PASSED` → exit 0. `NGROK_AUTHTOKEN` is the CI gate documented in the script + spec 17, intentionally **not** added to `.env.example` (same footgun reasoning as `OPENHAMMER_TUNNEL_E2E`). `npm run test:tunnel:ngrok` is gated/non-blocking (skips on any box lacking the authtoken or the binary — the hermetic trio never loads it). Trio green (619 passed / 1 skipped).

---

## Notes & risks for implementers

- **MCP SDK import paths resolve under NodeNext.** The SDK's `exports` map exposes a `./*` wildcard → `dist/esm/*`, so `@modelcontextprotocol/sdk/server/index.js`, `/types.js`, `/server/streamableHttp.js`, `/shared/transport.js` all resolve (files exist). Use them verbatim.
- **Per-request `Server` + `Transport`, no `sessionIdGenerator`** (stateless). `server.connect(transport as unknown as Transport)` cast absorbs SDK optional-callback type friction.
- **Header flush before `handleRequest`** is required or `@fastify/cors` expose-headers never reach the wire (browsers then strip the response). Flush Fastify's accumulated headers onto `reply.raw` before `handleRequest`.
- **`MAX_RESPONSE_BYTES` backstop replaces the ENTIRE content** (text or image) with one `response_too_large` JSON text block — a structured error, never a silent truncation. Compute bytes as text `Buffer.byteLength` + image `data.length`.
- **Result model is the spine.** Tool `execute` never throws for expected failures; only the `CallTool` handler narrows, and its `try/catch` is a bug safety-net only. Spawn-based tools (`bash`/`grep`/`find`/tunnel) manage their own streaming and return `Result` at the end — they do **not** use `io.ts` (fs-surface only).
- **`rg` + `fd` + `cloudflared` are presence-checked at runtime** (via `isToolAvailable`) with graceful `err`/`null` — no auto-download, no Node fallback, no install-time hard dep.
- **Image support is extension-based, no `sharp`** (no resize; oversized images hit the backstop). **No `diff`/`jose`/`sharp`/`dotenv` deps** — do not add them.
- **`npm audit` reports 5 high-severity advisories in `fast-uri`** (path-traversal/host-confusion), transitive via `fastify@4` → `@fastify/ajv-compiler`/`fast-json-stringify`. The only fix is `fastify@5` (breaking major; needs `@fastify/cors@10` + API changes). All MCP/Fastify wiring is written against v4 — so this is a **deliberate, deferred** major bump, not a scaffold concern. Do **not** `npm audit fix --force` (it major-bumps fastify and breaks the port). Revisit after v1 ships.

- **Biome 2.5 field note.** `linter.rules.recommended: true` is **deprecated** in Biome 2.5 (`biome check` warns on every run); use `linter.rules.preset: "recommended"` instead (`enum: recommended|all|none`). `01-scaffold-c` ships the non-deprecated form. This satisfies spec 01's "`recommended` preset" wording (field-agnostic) — do not hand-edit it back to `recommended: true`.


- **Spec `99` (filesystem-defined agent harness)** — future step-two design record. Do **not** implement or add tasks for it. v1 already keeps the door open (extensible `Server` capabilities, 7 fs/bash tools, truncation/backstop reused). Spec 18's `guide` tool (Phase A) orients clients now; the **sessions/memory substrate (spec 18 Phase B) is deferred** to `docs/agent-harness-design.md` and must be **reconciled with that doc's stateless thesis** before it's the right design (likely file-based / agent-as-directory, not server sessions). Promote spec 99 only after that reconciliation + v1 ship, verified end-to-end with a real MCP client.
- No LLM / agent loop / provider integration / **server-side session state** (stateless by design — see `docs/agent-harness-design.md` Parts 2 & 5A). Near-term cwd orientation is a stateless `guide` tool (spec 18 Phase A); per-session cwd/memory (spec 18 Phase B) is **deferred** and must be reconciled with the stateless thesis before actioning. No `Operations` interface seams. No `--sandbox` mode (isolation = containerize). No OAuth AS (only the well-known discovery pointer).
